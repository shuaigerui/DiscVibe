
import Foundation

import UIKit

class DV_SkillProfileView: UIView {
var enabledLeftResolve_dict: [String: Any]?
private var resultCaptionDismiss_list: [Any]!




    var skillCardTapHandler: (() -> Void)?
    var aiCardTapHandler: (() -> Void)?
    var ultimateTapHandler: (() -> Void)?

    override init(frame: CGRect) {
        super.init(frame: frame)
        setupUI()
        setupActions()
    }

    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

@discardableResult
 func documentGraphicsRevocationImageMutually(actionTrack: String!, local_1sTrack: Float, iconUrl: Double) -> [Any]! {
    var analyzingr: Double = 5.0
    _ = analyzingr
    var mediaX: Int = 5
    var firstJ: [Any]! = [String(cString: [103,111,111,103,0], encoding: .utf8)!, String(cString: [99,111,112,121,0], encoding: .utf8)!]
   for _ in 0 ..< 1 {
      analyzingr -= (Double(2 / (Swift.max(3, Int(analyzingr > 194788412.0 || analyzingr < -194788412.0 ? 52.0 : analyzingr)))))
   }
      mediaX |= firstJ.count
      mediaX -= mediaX
   return firstJ

}






    @objc private func aiCardTapped() {

         let aiterIsacfix: [Any]! = documentGraphicsRevocationImageMutually(actionTrack:String(cString: [108,115,98,102,117,108,108,95,55,95,56,48,0], encoding: .utf8)!, local_1sTrack:5.0, iconUrl:59.0)

      let aiterIsacfix_len = aiterIsacfix.count
      aiterIsacfix.enumerated().forEach({ (index,obj) in
          if index  >  42 {
                          print(obj)
          }
      })

_ = aiterIsacfix


       var f_vieww: Double = 5.0
    _ = f_vieww
      f_vieww /= Swift.max(5, (Double(Int(f_vieww > 266226116.0 || f_vieww < -266226116.0 ? 55.0 : f_vieww) / (Swift.max(9, Int(f_vieww > 316583048.0 || f_vieww < -316583048.0 ? 41.0 : f_vieww))))))

        aiCardTapHandler?()
    }

@discardableResult
 func cancelDurationOrientation() -> Double {
    var action4: [Any]! = [98, 16, 64]
    var unewsb: Double = 2.0
   for _ in 0 ..< 1 {
       var dimV: Bool = true
      withUnsafeMutablePointer(to: &dimV) { pointer in
    
      }
       var providerI: [Any]! = [String(cString: [117,115,97,98,108,101,0], encoding: .utf8)!, String(cString: [120,115,117,98,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &providerI) { pointer in
    
      }
         providerI.append(3)
         providerI.append(providerI.count)
         providerI = [(providerI.count & (dimV ? 1 : 3))]
      repeat {
         providerI.append(providerI.count)
         if providerI.count == 1333080 {
            break
         }
      } while (providerI.count == 1333080) && ((2 & providerI.count) > 3 && dimV)
      for _ in 0 ..< 2 {
         dimV = !dimV
      }
      while (dimV) {
          var calculatel: Double = 0.0
          _ = calculatel
         providerI = [((dimV ? 2 : 1) - 2)]
         calculatel += Double(3)
         break
      }
      unewsb /= Swift.max((Double((dimV ? 4 : 4) | 3)), 4)
   }
      action4.append(action4.count % 2)
     let markMerged: String! = String(cString: [97,114,98,105,116,114,97,114,121,0], encoding: .utf8)!
     var signCell: Double = 70.0
    var reducerAuthenticator:Double = 0
    signCell /= 2
    reducerAuthenticator /= Swift.max(Double(signCell), 1)

    return reducerAuthenticator

}






    private func setupUI() {

         let retainLock: Double = cancelDurationOrientation()

      print(retainLock)

_ = retainLock


       var iconj: [String: Any]! = [String(cString: [100,120,103,105,0], encoding: .utf8)!:18]
      iconj = ["\(iconj.keys.count)": iconj.keys.count + 2]

        addSubview(titleImageView)
        addSubview(iconImageView)
        addSubview(aiCardImageView)
        addSubview(skillCardImageView)
        addSubview(ultimateImageView)
        addSubview(eventsTitleImageView)

        titleImageView.snp.makeConstraints { make in
            make.leading.top.equalToSuperview().offset(16)
        }
        iconImageView.snp.makeConstraints { make in
            make.trailing.equalToSuperview()
            make.top.equalToSuperview()
        }
        aiCardImageView.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(16)
            make.top.equalTo(iconImageView.snp.bottom)
        }
        skillCardImageView.snp.makeConstraints { make in
            make.leading.equalTo(aiCardImageView.snp.trailing).offset(13)
            make.trailing.equalToSuperview().offset(-16)
            make.top.width.height.equalTo(aiCardImageView)
        }
        ultimateImageView.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(16)
            make.top.equalTo(aiCardImageView.snp.bottom).offset(16)
        }
        eventsTitleImageView.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(16)
            make.top.equalTo(ultimateImageView.snp.bottom).offset(16)
        }
    }

@discardableResult
 func symbolResourceSession() -> Double {
    var results1: Double = 5.0
    var spacingO: Double = 1.0
       var p_count9: String! = String(cString: [99,111,117,108,100,0], encoding: .utf8)!
       _ = p_count9
         p_count9 = "\(p_count9.count)"
      if p_count9 != p_count9 {
         p_count9.append("\(p_count9.count)")
      }
      repeat {
         p_count9 = "\(p_count9.count << (Swift.min(labs(2), 4)))"
         if (String(cString:[55,114,119,56,107,109,121,54,0], encoding: .utf8)!) == p_count9 {
            break
         }
      } while ((String(cString:[55,114,119,56,107,109,121,54,0], encoding: .utf8)!) == p_count9) && (1 > p_count9.count && p_count9 == String(cString:[55,0], encoding: .utf8)!)
      spacingO /= Swift.max(5, Double(3))
       var barR: Double = 5.0
       var followingL: String! = String(cString: [102,97,115,116,109,97,116,104,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &followingL) { pointer in
    
      }
         barR -= (Double(followingL == (String(cString:[54,0], encoding: .utf8)!) ? Int(barR > 289352764.0 || barR < -289352764.0 ? 83.0 : barR) : followingL.count))
      while (3.13 <= (2.22 + barR)) {
          var commenter1: Float = 4.0
          var lanJ: [String: Any]! = [String(cString: [110,111,110,110,117,108,108,115,115,114,99,0], encoding: .utf8)!:99.0]
          _ = lanJ
          var urlf: Int = 3
         followingL = "\(2)"
         commenter1 += Float(followingL.count + 3)
         lanJ = ["\(lanJ.keys.count)": (Int(commenter1 > 386807785.0 || commenter1 < -386807785.0 ? 5.0 : commenter1))]
         urlf -= followingL.count
         break
      }
         followingL.append("\((Int(barR > 342399333.0 || barR < -342399333.0 ? 51.0 : barR) * 1))")
      while (3.95 <= (barR / (Swift.max(Double(followingL.count), 9)))) {
         barR -= Double(followingL.count >> (Swift.min(labs(2), 4)))
         break
      }
      repeat {
         barR -= Double(1)
         if 794082.0 == barR {
            break
         }
      } while (2.77 > (barR * Double(followingL.count)) && 4.93 > (barR * 2.77)) && (794082.0 == barR)
         followingL.append("\((Int(barR > 178277853.0 || barR < -178277853.0 ? 25.0 : barR)))")
      results1 -= (Double(Int(results1 > 119191654.0 || results1 < -119191654.0 ? 66.0 : results1) * Int(spacingO > 235035599.0 || spacingO < -235035599.0 ? 54.0 : spacingO)))
     let max_f4Login: String! = String(cString: [109,111,100,101,108,115,0], encoding: .utf8)!
     let launchRed: String! = String(cString: [114,101,109,97,112,112,105,110,103,0], encoding: .utf8)!
     var textsDetails: Double = 47.0
    var encodeintraEncodembGnutls:Double = 0
    textsDetails = 5
    encodeintraEncodembGnutls /= Swift.max(textsDetails, 1)

    return encodeintraEncodembGnutls

}






    private func setupActions() {

         let keyHcmc: Double = symbolResourceSession()

      print(keyHcmc)

_ = keyHcmc


       var hangG: [Any]! = [String(cString: [102,105,110,97,108,105,122,105,110,103,0], encoding: .utf8)!, String(cString: [97,98,111,117,116,115,0], encoding: .utf8)!]
      hangG.append(1)

        let suffix = UITapGestureRecognizer(target: self, action: #selector(skillCardTapped))
        skillCardImageView.addGestureRecognizer(suffix)

        let shoulder = UITapGestureRecognizer(target: self, action: #selector(aiCardTapped))
        aiCardImageView.addGestureRecognizer(shoulder)

        let lhs = UITapGestureRecognizer(target: self, action: #selector(ultimateTapped))
        ultimateImageView.isUserInteractionEnabled = true
        ultimateImageView.addGestureRecognizer(lhs)
    }


    @objc private func skillCardTapped() {
       var loginL: Double = 1.0
    var encodedn: Bool = false
      loginL /= Swift.max((Double(1 << (Swift.min(labs(Int(loginL > 16420092.0 || loginL < -16420092.0 ? 94.0 : loginL)), 3)))), 5)
   if !encodedn || encodedn {
      encodedn = !encodedn
   }

        skillCardTapHandler?()
    }


    @objc private func ultimateTapped() {
       var price4: String! = String(cString: [100,105,97,103,114,97,109,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &price4) { pointer in
    
   }
      price4 = "\(price4.count)"

        ultimateTapHandler?()
    }

    private let titleImageView: UIImageView = {
       var ultimateo: String! = String(cString: [112,114,101,118,0], encoding: .utf8)!
    var skillZ: Bool = true
   withUnsafeMutablePointer(to: &skillZ) { pointer in
          _ = pointer.pointee
   }
       var score8: Bool = true
       var signH: Int = 4
       _ = signH
         signH |= ((score8 ? 4 : 4) << (Swift.min(labs(signH), 4)))
       var attributesw: Float = 4.0
       var rhsx: Float = 5.0
       _ = rhsx
       var defaultsV: [String: Any]! = [String(cString: [97,100,106,117,115,116,0], encoding: .utf8)!:String(cString: [109,112,101,103,112,105,99,116,117,114,101,0], encoding: .utf8)!, String(cString: [115,117,99,99,101,115,115,102,117,108,0], encoding: .utf8)!:String(cString: [117,110,112,105,110,0], encoding: .utf8)!]
      for _ in 0 ..< 1 {
         rhsx -= (Float(Int(rhsx > 91447527.0 || rhsx < -91447527.0 ? 60.0 : rhsx) - 1))
      }
      for _ in 0 ..< 1 {
         attributesw -= Float(signH)
      }
         attributesw += Float(defaultsV.values.count ^ 3)
      ultimateo = "\((2 >> (Swift.min(labs((score8 ? 5 : 4)), 1))))"

        let imageView = UIImageView(image: UIImage(named: "home_title"))
       var backb: String! = String(cString: [118,97,108,105,100,97,116,97,98,108,101,0], encoding: .utf8)!
      repeat {
          var photob: Int = 0
          var viewsp: [Any]! = [63, 99]
          var local_s2: Double = 1.0
         withUnsafeMutablePointer(to: &local_s2) { pointer in
                _ = pointer.pointee
         }
          var ownk: Double = 2.0
          var cancel8: Int = 5
         backb = "\(((String(cString:[50,0], encoding: .utf8)!) == backb ? backb.count : Int(local_s2 > 364839479.0 || local_s2 < -364839479.0 ? 5.0 : local_s2)))"
         photob -= 3 + backb.count
         viewsp.append(cancel8 ^ 1)
         ownk -= Double(viewsp.count)
         cancel8 &= (Int(ownk > 153755562.0 || ownk < -153755562.0 ? 94.0 : ownk))
         if backb == (String(cString:[114,98,109,105,113,113,104,0], encoding: .utf8)!) {
            break
         }
      } while (backb != String(cString:[68,0], encoding: .utf8)! || 2 == backb.count) && (backb == (String(cString:[114,98,109,105,113,113,104,0], encoding: .utf8)!))
      for _ in 0 ..< 1 {
          var currentn: Bool = false
          var cancelledx: String! = String(cString: [111,99,117,109,101,110,116,0], encoding: .utf8)!
          _ = cancelledx
         backb = "\(((currentn ? 3 : 4)))"
         cancelledx = "\(cancelledx.count ^ backb.count)"
      }
       var real0: Int = 0
       _ = real0
         real0 |= 2 - backb.count
      skillZ = backb.hasSuffix("\(skillZ)")
        imageView.contentMode = .scaleAspectFill
        return imageView
    }()

    private let iconImageView: UIImageView = {
       var des8: String! = String(cString: [100,105,115,115,99,111,110,110,101,99,116,0], encoding: .utf8)!
      des8 = "\((des8 == (String(cString:[76,0], encoding: .utf8)!) ? des8.count : des8.count))"

        let imageView = UIImageView(image: UIImage(named: "home_icon"))
        imageView.contentMode = .scaleAspectFill
        return imageView
    }()

    private let aiCardImageView: UIImageView = {
       var uploadU: String! = String(cString: [97,118,102,111,114,109,97,116,114,101,115,0], encoding: .utf8)!
    _ = uploadU
   for _ in 0 ..< 3 {
      uploadU = "\(2 << (Swift.min(3, uploadU.count)))"
   }

        let imageView = UIImageView(image: UIImage(named: "home_ai"))
        imageView.isUserInteractionEnabled = true
        return imageView
    }()

    private let skillCardImageView: UIImageView = {
       var rowj: Int = 3
   if 2 > (rowj | rowj) {
      rowj /= Swift.max(2, 2)
   }

        let imageView = UIImageView(image: UIImage(named: "home_skill"))
        imageView.isUserInteractionEnabled = true
        return imageView
    }()

    private let ultimateImageView: UIImageView = {
       var names7: String! = String(cString: [116,101,109,112,108,97,116,101,100,0], encoding: .utf8)!
       var poolb: String! = String(cString: [100,97,116,97,116,121,112,101,115,0], encoding: .utf8)!
       var selected5: String! = String(cString: [112,111,108,105,99,101,0], encoding: .utf8)!
       var optionw: String! = String(cString: [99,114,111,115,115,102,97,100,105,110,103,0], encoding: .utf8)!
      repeat {
          var spreadsM: Double = 1.0
          var sessionv: String! = String(cString: [118,112,114,105,110,116,102,0], encoding: .utf8)!
          var authorization8: String! = String(cString: [114,97,110,103,101,115,0], encoding: .utf8)!
         selected5 = "\(1)"
         spreadsM += (Double(poolb.count << (Swift.min(1, labs(Int(spreadsM > 385402018.0 || spreadsM < -385402018.0 ? 65.0 : spreadsM))))))
         sessionv.append("\(sessionv.count)")
         authorization8 = "\(2 | optionw.count)"
         if selected5 == (String(cString:[120,121,107,107,102,107,0], encoding: .utf8)!) {
            break
         }
      } while (poolb != selected5) && (selected5 == (String(cString:[120,121,107,107,102,107,0], encoding: .utf8)!))
      while (optionw.count == 1) {
         selected5 = "\(poolb.count * 2)"
         break
      }
      for _ in 0 ..< 1 {
         optionw.append("\(((String(cString:[118,0], encoding: .utf8)!) == poolb ? poolb.count : selected5.count))")
      }
      repeat {
         optionw = "\(optionw.count >> (Swift.min(4, poolb.count)))"
         if optionw == (String(cString:[108,114,97,50,54,101,116,0], encoding: .utf8)!) {
            break
         }
      } while (optionw != String(cString:[102,0], encoding: .utf8)! && selected5.count <= 5) && (optionw == (String(cString:[108,114,97,50,54,101,116,0], encoding: .utf8)!))
      repeat {
         optionw.append("\(optionw.count)")
         if 2078326 == optionw.count {
            break
         }
      } while (2078326 == optionw.count) && (!optionw.hasSuffix("\(selected5.count)"))
      while (selected5.hasPrefix(poolb)) {
         poolb.append("\(poolb.count >> (Swift.min(selected5.count, 3)))")
         break
      }
      if poolb != String(cString:[71,0], encoding: .utf8)! || selected5 != String(cString:[106,0], encoding: .utf8)! {
         poolb = "\(optionw.count % (Swift.max(1, 2)))"
      }
          var idsR: String! = String(cString: [117,110,100,101,114,115,99,111,114,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &idsR) { pointer in
                _ = pointer.pointee
         }
         poolb = "\(((String(cString:[88,0], encoding: .utf8)!) == optionw ? idsR.count : optionw.count))"
         poolb = "\((selected5 == (String(cString:[102,0], encoding: .utf8)!) ? optionw.count : selected5.count))"
      names7.append("\(poolb.count * selected5.count)")

        let imageView = UIImageView(image: UIImage(named: "home_ultimate"))
        return imageView
    }()

    private let eventsTitleImageView: UIImageView = {
       var cellu: Double = 1.0
    var ratio0: Int = 1
      cellu /= Swift.max(Double(ratio0), 3)

        let imageView = UIImageView(image: UIImage(named: "home_events"))
      cellu += (Double(Int(cellu > 137381216.0 || cellu < -137381216.0 ? 80.0 : cellu)))
        imageView.contentMode = .scaleAspectFill
      ratio0 |= 2 | ratio0
        return imageView
    }()
}
