
import Foundation

import AVFoundation
import PhotosUI
import SVProgressHUD
import Toast_Swift
import UIKit
import UniformTypeIdentifiers

class DV_ApplyController: DV_MainDelegateController {
var max_62: Float? = 0.0
private var locationInitialsStartArr: [Any]?
var viewsSpace: Float? = 0.0


private var fieldModel: DV_UserObject!


    private let maxVideoFileSize: Int64 = 200 * 1024 * 1024
    private let analysisTimeout: TimeInterval = 20

    private var selectedVideoURL: URL?
    private var poseAnalyzer = DV_TabbarInput()
    private var analysisTimeoutWorkItem: DispatchWorkItem?
    private var isAnalyzing = false
    private var isAnalysisCancelled = false
    private let onDeviceAnalysisConsentKey = "dv_on_device_form_analysis_consent"

@discardableResult
 func launchObservationVisibleDecimalRegularLabel(secondsColor: [String: Any]!) -> UILabel! {
    var deletedZ: Double = 5.0
    var roww: Int = 5
   withUnsafeMutablePointer(to: &roww) { pointer in
          _ = pointer.pointee
   }
   repeat {
       var priceh: [Any]! = [10.0]
       var angleq: [Any]! = [String(cString: [102,97,115,116,0], encoding: .utf8)!]
       var successr: Bool = true
       _ = successr
       var safeH: String! = String(cString: [121,111,117,114,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &safeH) { pointer in
             _ = pointer.pointee
      }
       var configurationk: Bool = true
         successr = (priceh.contains { $0 as? Bool == configurationk })
          var likesT: [String: Any]! = [String(cString: [98,108,97,107,101,0], encoding: .utf8)!:50, String(cString: [97,110,105,109,97,116,105,111,110,0], encoding: .utf8)!:66]
         withUnsafeMutablePointer(to: &likesT) { pointer in
    
         }
         priceh = [safeH.count << (Swift.min(5, angleq.count))]
         likesT = ["\(likesT.values.count)": (likesT.count & (successr ? 1 : 4))]
       var updated7: [Any]! = [String(cString: [116,119,101,97,107,0], encoding: .utf8)!, String(cString: [116,111,117,99,104,101,115,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &updated7) { pointer in
             _ = pointer.pointee
      }
       var configuration3: [Any]! = [80, 44, 14]
      withUnsafeMutablePointer(to: &configuration3) { pointer in
             _ = pointer.pointee
      }
      for _ in 0 ..< 1 {
         updated7.append(configuration3.count)
      }
      repeat {
         successr = (priceh.contains { $0 as? String == "\(configuration3.last!)" })
         if successr ? !successr : successr {
            break
         }
      } while (5 == (1 * priceh.count) || successr) && (successr ? !successr : successr)
         updated7.append(2 >> (Swift.min(1, configuration3.count)))
         configuration3.append(3 * configuration3.count)
         updated7.append(((configurationk ? 5 : 2) << (Swift.min(angleq.count, 2))))
         configurationk = (priceh.count << (Swift.min(configuration3.count, 2))) <= 88
      while ((updated7.count << (Swift.min(labs(5), 1))) >= 3 && 2 >= (5 << (Swift.min(2, configuration3.count)))) {
         updated7 = [updated7.count]
         break
      }
      if safeH.hasSuffix("\(configurationk)") {
          var modalT: Bool = false
          var loading5: String! = String(cString: [103,110,117,116,108,115,0], encoding: .utf8)!
          var v_positionR: Int = 3
          var setupC: Double = 4.0
         withUnsafeMutablePointer(to: &setupC) { pointer in
    
         }
         configurationk = !successr
         modalT = 36 == loading5.count
         loading5 = "\(priceh.count)"
         v_positionR -= ((successr ? 2 : 5))
         setupC *= (Double((modalT ? 4 : 5)))
      }
      repeat {
         configuration3.append((priceh.count ^ (configurationk ? 1 : 2)))
         if 2977703 == configuration3.count {
            break
         }
      } while (2977703 == configuration3.count) && (!safeH.contains("\(configuration3.count)"))
      repeat {
         priceh = [2]
         if priceh.count == 31574 {
            break
         }
      } while (safeH.contains("\(priceh.count)")) && (priceh.count == 31574)
       var spreads_: Float = 0.0
       var introduceP: Float = 3.0
      withUnsafeMutablePointer(to: &introduceP) { pointer in
    
      }
      for _ in 0 ..< 3 {
         configurationk = (Float(angleq.count) * introduceP) > 48.77
      }
         spreads_ /= Swift.max(5, (Float((String(cString:[119,0], encoding: .utf8)!) == safeH ? Int(introduceP > 182175147.0 || introduceP < -182175147.0 ? 91.0 : introduceP) : safeH.count)))
      roww >>= Swift.min(3, labs(((successr ? 1 : 4))))
      if roww == 1520419 {
         break
      }
   } while (roww == 1520419) && (3 > (Int(deletedZ > 117872453.0 || deletedZ < -117872453.0 ? 3.0 : deletedZ) - roww) && 5 > (roww / 3))
      deletedZ -= Double(2)
     let hadTips: [String: Any]! = [String(cString: [112,97,103,101,104,97,115,104,0], encoding: .utf8)!:62, String(cString: [97,121,117,118,108,101,0], encoding: .utf8)!:26]
     var normalReuse: UIImageView! = UIImageView()
     var modalCollection: UIView! = UIView()
     let audioIntroduction: Double = 82.0
    var utimeIsacfixDatas:UILabel! = UILabel(frame:CGRect(x: 301, y: 281, width: 0, height: 0))
    utimeIsacfixDatas.textAlignment = .center
    utimeIsacfixDatas.font = UIFont.systemFont(ofSize:17)
    utimeIsacfixDatas.text = ""
    utimeIsacfixDatas.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    utimeIsacfixDatas.frame = CGRect(x: 63, y: 229, width: 0, height: 0)
    utimeIsacfixDatas.alpha = 0.8;
    utimeIsacfixDatas.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    normalReuse.frame = CGRect(x: 297, y: 181, width: 0, height: 0)
    normalReuse.alpha = 0.0;
    normalReuse.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    normalReuse.animationRepeatCount = 1
    normalReuse.image = UIImage(named:String(cString: [115,99,114,101,101,110,0], encoding: .utf8)!)
    normalReuse.contentMode = .scaleAspectFit
    
    modalCollection.frame = CGRect(x: 79, y: 24, width: 0, height: 0)
    modalCollection.alpha = 0.7;
    modalCollection.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    

    
    return utimeIsacfixDatas

}






    private func presentVideoPicker() {

         let objectThawed: UILabel! = launchObservationVisibleDecimalRegularLabel(secondsColor:[String(cString: [109,115,103,115,109,100,101,99,0], encoding: .utf8)!:0, String(cString: [98,105,112,114,101,100,0], encoding: .utf8)!:81, String(cString: [111,108,97,110,97,0], encoding: .utf8)!:17])

      if objectThawed != nil {
          let objectThawed_tag = objectThawed.tag
          self.view.addSubview(objectThawed)
      }

_ = objectThawed


       var s_positionF: [String: Any]! = [String(cString: [99,111,108,114,97,109,0], encoding: .utf8)!:[93, 92, 20]]
       var paget: Double = 1.0
      withUnsafeMutablePointer(to: &paget) { pointer in
             _ = pointer.pointee
      }
       var d_alphao: [String: Any]! = [String(cString: [102,111,108,108,111,119,115,0], encoding: .utf8)!:String(cString: [99,111,117,110,116,101,114,115,0], encoding: .utf8)!, String(cString: [119,105,100,101,102,101,108,101,109,0], encoding: .utf8)!:String(cString: [105,102,105,108,116,101,114,0], encoding: .utf8)!]
         d_alphao = ["\(d_alphao.count)": d_alphao.values.count * 1]
          var introductionj: String! = String(cString: [115,117,98,112,97,99,107,101,116,115,0], encoding: .utf8)!
         paget -= Double(d_alphao.values.count - 2)
         introductionj = "\(introductionj.count)"
      for _ in 0 ..< 2 {
         paget += Double(1 >> (Swift.min(4, d_alphao.keys.count)))
      }
      if 4 >= (d_alphao.values.count ^ 5) {
         d_alphao["\(paget)"] = d_alphao.count
      }
         paget += Double(1)
      if (d_alphao.count << (Swift.min(labs(3), 4))) >= 2 {
         d_alphao["\(paget)"] = d_alphao.count
      }
      s_positionF["\(s_positionF.values.count)"] = 2 >> (Swift.min(1, s_positionF.count))

        var progress = PHPickerConfiguration()
        progress.filter = .videos
        progress.selectionLimit = 1

        let cancel = PHPickerViewController(configuration: progress)
        cancel.delegate = self
        present(cancel, animated: true)
    }

@discardableResult
 func appropriateGreenFoundImage(ultimateConfig: [String: Any]!) -> Float {
    var usernameY: String! = String(cString: [112,114,101,102,105,120,101,100,0], encoding: .utf8)!
    var jointh: Int = 2
   withUnsafeMutablePointer(to: &jointh) { pointer in
    
   }
    var keyK: Float = 5.0
    _ = keyK
   for _ in 0 ..< 1 {
      keyK -= (Float(Int(keyK > 79998931.0 || keyK < -79998931.0 ? 7.0 : keyK)))
   }
      keyK /= Swift.max(Float(3 >> (Swift.min(5, labs(jointh)))), 5)
   while (2 < (5 ^ jointh) && 1 < (usernameY.count ^ 5)) {
       var menuV: Float = 4.0
      withUnsafeMutablePointer(to: &menuV) { pointer in
             _ = pointer.pointee
      }
       var nextq: Bool = true
      withUnsafeMutablePointer(to: &nextq) { pointer in
    
      }
         nextq = !nextq || 18.35 == menuV
      if !nextq {
         menuV -= (Float(3 & Int(menuV > 107817762.0 || menuV < -107817762.0 ? 8.0 : menuV)))
      }
         nextq = !nextq
         menuV /= Swift.max((Float((nextq ? 1 : 5) + Int(menuV > 218533097.0 || menuV < -218533097.0 ? 17.0 : menuV))), 2)
          var formatterJ: String! = String(cString: [97,108,112,104,97,110,117,109,101,114,105,99,0], encoding: .utf8)!
          var white9: Bool = true
         nextq = 40.72 > menuV || nextq
         formatterJ.append("\((Int(menuV > 277497563.0 || menuV < -277497563.0 ? 85.0 : menuV) % 2))")
         white9 = !white9
      repeat {
         menuV -= (Float((nextq ? 3 : 1) % (Swift.max(Int(menuV > 336386327.0 || menuV < -336386327.0 ? 40.0 : menuV), 2))))
         if 1008169.0 == menuV {
            break
         }
      } while (3.33 <= (4.66 / (Swift.max(4, menuV)))) && (1008169.0 == menuV)
      jointh &= usernameY.count & 1
      break
   }
   return keyK

}






    private func startAnalysis(for url: URL) {

         var castCooldowns: Float = appropriateGreenFoundImage(ultimateConfig:[String(cString: [99,111,114,110,101,114,0], encoding: .utf8)!:44, String(cString: [102,111,114,99,101,100,0], encoding: .utf8)!:77, String(cString: [105,110,112,117,116,0], encoding: .utf8)!:67])

      print(castCooldowns)

withUnsafeMutablePointer(to: &castCooldowns) { pointer in
    
}


       var blue8: Bool = true
    var add2: [String: Any]! = [String(cString: [111,100,100,97,118,103,0], encoding: .utf8)!:56, String(cString: [115,105,120,0], encoding: .utf8)!:20]
   repeat {
       var totalx: Float = 1.0
       var tinitialsD: String! = String(cString: [113,115,118,100,101,105,110,116,0], encoding: .utf8)!
       var prefix_rP: String! = String(cString: [115,112,114,105,116,101,0], encoding: .utf8)!
      if totalx == 5.37 {
         totalx -= Float(tinitialsD.count)
      }
      while (tinitialsD.contains("\(totalx)")) {
         totalx += (Float(prefix_rP == (String(cString:[114,0], encoding: .utf8)!) ? tinitialsD.count : prefix_rP.count))
         break
      }
      repeat {
         totalx -= Float(prefix_rP.count)
         if 1810170.0 == totalx {
            break
         }
      } while (5.99 <= (totalx + 1.8)) && (1810170.0 == totalx)
         totalx += (Float(2 / (Swift.max(8, Int(totalx > 28333849.0 || totalx < -28333849.0 ? 22.0 : totalx)))))
         totalx -= Float(3)
      for _ in 0 ..< 1 {
          var actionz: Float = 4.0
          _ = actionz
          var modalj: Float = 2.0
          var fallbackM: String! = String(cString: [111,118,101,114,114,105,100,101,110,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &fallbackM) { pointer in
    
         }
          var componentsl: Float = 3.0
          var contactG: Double = 2.0
         prefix_rP.append("\((prefix_rP == (String(cString:[48,0], encoding: .utf8)!) ? prefix_rP.count : Int(actionz > 363554935.0 || actionz < -363554935.0 ? 56.0 : actionz)))")
         modalj -= (Float(Int(modalj > 108778989.0 || modalj < -108778989.0 ? 75.0 : modalj)))
         fallbackM.append("\((Int(contactG > 391928547.0 || contactG < -391928547.0 ? 64.0 : contactG) >> (Swift.min(prefix_rP.count, 5))))")
         componentsl /= Swift.max(5, (Float(Int(contactG > 231251742.0 || contactG < -231251742.0 ? 71.0 : contactG) & Int(componentsl > 279596378.0 || componentsl < -279596378.0 ? 2.0 : componentsl))))
      }
       var analysish: Bool = true
      withUnsafeMutablePointer(to: &analysish) { pointer in
    
      }
       var window_hQ: Bool = true
      for _ in 0 ..< 2 {
         window_hQ = analysish && prefix_rP.count <= 58
      }
         analysish = totalx < 1.62
      add2 = ["\(add2.keys.count)": add2.keys.count]
      if add2.count == 3227621 {
         break
      }
   } while (add2.count == 3227621) && (3 >= add2.values.count && 3 >= (add2.values.count | 3))

   for _ in 0 ..< 3 {
      add2 = ["\(add2.count)": 2 % (Swift.max(9, add2.values.count))]
   }
        cancelAnalysis(showFailureToast: false)
       var baseX: String! = String(cString: [105,110,105,116,105,97,108,105,122,101,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &baseX) { pointer in
             _ = pointer.pointee
      }
          var thumbnailJ: Int = 0
          var generatorr: [String: Any]! = [String(cString: [112,114,101,99,104,101,99,107,111,117,116,0], encoding: .utf8)!:[String(cString: [115,116,101,112,0], encoding: .utf8)!, String(cString: [99,97,110,99,101,108,97,116,105,111,110,0], encoding: .utf8)!]]
          var control9: [Any]! = [82.0]
         baseX.append("\(1 & control9.count)")
         thumbnailJ -= 2 + control9.count
         generatorr["\(control9.count)"] = generatorr.count
      while (baseX.count <= baseX.count) {
         baseX = "\(baseX.count)"
         break
      }
      for _ in 0 ..< 2 {
         baseX = "\(2)"
      }
      blue8 = (((blue8 ? baseX.count : 61) & baseX.count) == 61)

        isAnalyzing = true
        isAnalysisCancelled = false
        poseAnalyzer = DV_TabbarInput()

        SVProgressHUD.setDefaultMaskType(.black)
        SVProgressHUD.show(withStatus: "Checking form on device...")

        let alreadyItem = DispatchWorkItem { [weak self] in
            self?.handleAnalysisTimeout()
        }
        analysisTimeoutWorkItem = alreadyItem
        DispatchQueue.main.asyncAfter(deadline: .now() + analysisTimeout, execute: alreadyItem)

        poseAnalyzer.analyze(videoURL: url) { [weak self] result in
            self?.handleAnalysisCompletion(result)
        }
    }

@discardableResult
 func sendPersonFoundation(summaryThumbnail: String!) -> [Any]! {
    var keysM: Double = 1.0
    var shopI: String! = String(cString: [116,105,112,0], encoding: .utf8)!
    var inputA: [Any]! = [78, 100, 11]
   while (keysM < 3.78) {
       var removez: String! = String(cString: [100,97,114,116,115,0], encoding: .utf8)!
       _ = removez
       var deviceQ: String! = String(cString: [114,101,109,97,105,110,105,110,103,0], encoding: .utf8)!
          var handler0: String! = String(cString: [99,111,110,116,114,111,108,0], encoding: .utf8)!
         deviceQ = "\(removez.count)"
         handler0 = "\(deviceQ.count + 2)"
         deviceQ.append("\(deviceQ.count)")
       var sampled: Bool = true
      while (2 <= removez.count) {
         removez = "\((deviceQ == (String(cString:[88,0], encoding: .utf8)!) ? (sampled ? 4 : 1) : deviceQ.count))"
         break
      }
      while (deviceQ.count > 1) {
         deviceQ = "\(((sampled ? 1 : 2)))"
         break
      }
      while (removez.contains("\(sampled)")) {
          var reusek: Int = 1
         sampled = !sampled || removez.count >= 53
         reusek += deviceQ.count - 2
         break
      }
      inputA = [2]
      break
   }
   while ((inputA.count % (Swift.max(3, 5))) <= 2 && (inputA.count % (Swift.max(shopI.count, 1))) <= 3) {
       var componentsx: String! = String(cString: [117,116,117,114,101,0], encoding: .utf8)!
       _ = componentsx
       var partnerP: [Any]! = [69, 7]
       _ = partnerP
         partnerP.append(partnerP.count)
          var collection1: String! = String(cString: [98,108,117,101,0], encoding: .utf8)!
          var buttonh: [Any]! = [[String(cString: [105,110,99,111,114,114,101,99,116,0], encoding: .utf8)!:String(cString: [115,104,97,114,101,115,0], encoding: .utf8)!, String(cString: [111,112,116,105,109,105,115,109,0], encoding: .utf8)!:String(cString: [116,114,97,110,115,105,116,105,111,110,105,110,103,0], encoding: .utf8)!, String(cString: [101,116,105,109,101,0], encoding: .utf8)!:String(cString: [99,111,108,117,109,115,0], encoding: .utf8)!]]
         componentsx = "\(collection1.count)"
         buttonh.append(componentsx.count << (Swift.min(labs(3), 3)))
          var size_p1: Bool = true
         componentsx = "\((partnerP.count - (size_p1 ? 1 : 3)))"
          var updatesn: Double = 1.0
          var momentN: Double = 5.0
          var previewA: Int = 3
         partnerP = [(Int(updatesn > 148817224.0 || updatesn < -148817224.0 ? 6.0 : updatesn) + 3)]
         momentN += Double(previewA << (Swift.min(componentsx.count, 5)))
         previewA += (Int(momentN > 341719362.0 || momentN < -341719362.0 ? 36.0 : momentN) / (Swift.max(Int(updatesn > 388575358.0 || updatesn < -388575358.0 ? 70.0 : updatesn), 7)))
         componentsx = "\((componentsx == (String(cString:[88,0], encoding: .utf8)!) ? partnerP.count : componentsx.count))"
       var introI: Double = 4.0
         introI += Double(partnerP.count - 1)
      shopI = "\(shopI.count | 3)"
      break
   }
   return inputA

}






    private func fileSize(at url: URL) -> Int64? {

         var polygonSavepoints: [Any]! = sendPersonFoundation(summaryThumbnail:String(cString: [115,116,114,103,108,111,98,0], encoding: .utf8)!)

      let polygonSavepoints_len = polygonSavepoints.count
      polygonSavepoints.forEach({ (obj) in
          print(obj)
      })

withUnsafeMutablePointer(to: &polygonSavepoints) { pointer in
        _ = pointer.pointee
}


       var fingerprintR: Double = 5.0
   if 5.12 <= fingerprintR {
      fingerprintR += (Double(Int(fingerprintR > 81245331.0 || fingerprintR < -81245331.0 ? 48.0 : fingerprintR) << (Swift.min(5, labs(Int(fingerprintR > 28888708.0 || fingerprintR < -28888708.0 ? 24.0 : fingerprintR))))))
   }

        guard let attributes = try? FileManager.default.attributesOfItem(atPath: url.path),
              let size = attributes[.size] as? NSNumber else {
            return nil
        }
        return size.int64Value
    }

@discardableResult
 func restoreRectWhiteClear(time__Photo: [Any]!, alreadySuffix: [String: Any]!) -> Bool {
    var navigation7: Double = 0.0
    var reuset: String! = String(cString: [97,108,105,97,115,101,115,0], encoding: .utf8)!
    var delegate_ncb: Bool = true
   for _ in 0 ..< 1 {
       var likei: [Any]! = [99, 69, 58]
       var commentb: Double = 4.0
      withUnsafeMutablePointer(to: &commentb) { pointer in
             _ = pointer.pointee
      }
       var providert: String! = String(cString: [99,111,112,121,116,111,0], encoding: .utf8)!
       var dismiss5: String! = String(cString: [103,108,111,98,97,108,108,121,0], encoding: .utf8)!
       var auth1: String! = String(cString: [97,99,99,101,112,116,101,100,0], encoding: .utf8)!
         providert = "\((3 | Int(commentb > 65491028.0 || commentb < -65491028.0 ? 23.0 : commentb)))"
      while (dismiss5.count < 1) {
          var container5: String! = String(cString: [99,105,114,99,108,101,100,0], encoding: .utf8)!
          _ = container5
          var checkP: String! = String(cString: [116,104,111,117,103,104,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &checkP) { pointer in
    
         }
          var docz: String! = String(cString: [114,97,116,105,111,0], encoding: .utf8)!
          _ = docz
          var release_mR: Int = 5
         withUnsafeMutablePointer(to: &release_mR) { pointer in
                _ = pointer.pointee
         }
          var controlC: Double = 4.0
          _ = controlC
         dismiss5 = "\(auth1.count + providert.count)"
         container5 = "\(checkP.count << (Swift.min(docz.count, 5)))"
         checkP.append("\((2 * Int(commentb > 106785965.0 || commentb < -106785965.0 ? 41.0 : commentb)))")
         docz.append("\(auth1.count - 1)")
         release_mR |= 1 % (Swift.max(4, container5.count))
         controlC -= Double(providert.count ^ likei.count)
         break
      }
         commentb *= Double(2 - auth1.count)
         likei = [((String(cString:[106,0], encoding: .utf8)!) == providert ? providert.count : auth1.count)]
      while (dismiss5.count == likei.count) {
          var reporto: Bool = false
         withUnsafeMutablePointer(to: &reporto) { pointer in
    
         }
          var size_84a: String! = String(cString: [115,97,109,112,108,101,114,0], encoding: .utf8)!
         dismiss5 = "\(2)"
         size_84a.append("\(((String(cString:[95,0], encoding: .utf8)!) == auth1 ? (reporto ? 2 : 2) : auth1.count))")
         break
      }
          var right4: String! = String(cString: [97,98,115,100,105,102,102,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &right4) { pointer in
    
         }
          var reviseL: [Any]! = [String(cString: [111,118,101,114,108,97,112,0], encoding: .utf8)!, String(cString: [107,97,108,109,97,110,0], encoding: .utf8)!]
         auth1.append("\(2)")
         right4 = "\((auth1 == (String(cString:[112,0], encoding: .utf8)!) ? auth1.count : reviseL.count))"
         reviseL = [(auth1 == (String(cString:[104,0], encoding: .utf8)!) ? reviseL.count : auth1.count)]
         likei.append((auth1 == (String(cString:[66,0], encoding: .utf8)!) ? auth1.count : likei.count))
      if 5 <= (dismiss5.count & 5) || (Double(dismiss5.count) * commentb) <= 1.43 {
         commentb -= (Double(providert == (String(cString:[52,0], encoding: .utf8)!) ? Int(commentb > 104676537.0 || commentb < -104676537.0 ? 81.0 : commentb) : providert.count))
      }
         providert = "\(2)"
      repeat {
         providert = "\(providert.count)"
         if providert.count == 3720316 {
            break
         }
      } while (providert.count >= likei.count) && (providert.count == 3720316)
      while (dismiss5 != String(cString:[107,0], encoding: .utf8)!) {
         auth1 = "\(dismiss5.count & 2)"
         break
      }
      while (5 <= auth1.count) {
         auth1 = "\(likei.count << (Swift.min(labs(3), 3)))"
         break
      }
      for _ in 0 ..< 2 {
         auth1.append("\(2)")
      }
      while ((commentb + 1.96) >= 2.81 && 3 >= (4 - dismiss5.count)) {
         commentb -= Double(1 & providert.count)
         break
      }
         dismiss5 = "\(1 >> (Swift.min(3, dismiss5.count)))"
      navigation7 /= Swift.max(Double(3 << (Swift.min(4, dismiss5.count))), 5)
   }
   for _ in 0 ..< 1 {
       var analysis5: Int = 5
       var passwordk: String! = String(cString: [100,118,98,115,117,98,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &passwordk) { pointer in
    
      }
       var j_alpha4: [Any]! = [String(cString: [110,116,112,0], encoding: .utf8)!, String(cString: [115,108,97,115,104,0], encoding: .utf8)!, String(cString: [101,110,99,108,97,118,101,0], encoding: .utf8)!]
       var tabJ: Double = 1.0
         analysis5 |= passwordk.count
          var usernameA: Int = 5
         withUnsafeMutablePointer(to: &usernameA) { pointer in
                _ = pointer.pointee
         }
         j_alpha4 = [(3 << (Swift.min(labs(Int(tabJ > 283542203.0 || tabJ < -283542203.0 ? 10.0 : tabJ)), 4)))]
         usernameA %= Swift.max(1, (Int(tabJ > 231965193.0 || tabJ < -231965193.0 ? 2.0 : tabJ) & 1))
      if j_alpha4.contains { $0 as? Double == tabJ } {
         j_alpha4.append(3 << (Swift.min(5, passwordk.count)))
      }
         tabJ /= Swift.max(4, (Double(passwordk == (String(cString:[76,0], encoding: .utf8)!) ? passwordk.count : j_alpha4.count)))
      if (passwordk.count << (Swift.min(labs(2), 2))) < 5 && (2 << (Swift.min(5, j_alpha4.count))) < 1 {
         j_alpha4 = [2]
      }
         tabJ /= Swift.max(Double(j_alpha4.count), 4)
          var lhsk: Double = 4.0
          var destinationf: [Any]! = [99, 49]
         j_alpha4 = [j_alpha4.count + 1]
         lhsk += Double(j_alpha4.count)
         destinationf = [3 << (Swift.min(labs(analysis5), 2))]
          var ageY: String! = String(cString: [105,110,111,100,101,0], encoding: .utf8)!
          var x_positionN: [String: Any]! = [String(cString: [114,101,102,99,111,117,110,116,101,100,111,98,106,101,99,116,0], encoding: .utf8)!:39, String(cString: [111,102,111,114,109,97,116,0], encoding: .utf8)!:85, String(cString: [114,101,109,105,110,100,0], encoding: .utf8)!:55]
         tabJ /= Swift.max(Double(x_positionN.keys.count), 1)
         ageY = "\(x_positionN.values.count)"
         j_alpha4.append(passwordk.count)
       var allt: String! = String(cString: [97,110,110,111,116,97,116,105,111,110,115,0], encoding: .utf8)!
       var backgroundg: [String: Any]! = [String(cString: [100,105,115,116,0], encoding: .utf8)!:String(cString: [115,117,98,98,108,111,99,107,115,0], encoding: .utf8)!, String(cString: [99,108,111,99,107,119,105,115,101,0], encoding: .utf8)!:String(cString: [114,101,103,117,108,97,114,0], encoding: .utf8)!, String(cString: [112,111,108,121,0], encoding: .utf8)!:String(cString: [109,101,109,111,106,105,0], encoding: .utf8)!]
       var playingT: [String: Any]! = [String(cString: [114,101,99,111,109,112,111,115,101,0], encoding: .utf8)!:54.0]
       _ = playingT
      if passwordk.count >= 4 {
          var remoteN: Double = 4.0
          var emptyC: String! = String(cString: [115,117,98,109,101,115,115,97,103,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &emptyC) { pointer in
    
         }
          var messagesp: String! = String(cString: [97,109,117,108,116,105,0], encoding: .utf8)!
         passwordk.append("\(j_alpha4.count)")
         remoteN /= Swift.max(5, Double(3))
         emptyC.append("\(j_alpha4.count)")
         messagesp.append("\(emptyC.count << (Swift.min(labs(2), 3)))")
      }
         allt.append("\(allt.count / 2)")
         backgroundg["\(passwordk)"] = 2
         playingT = ["\(j_alpha4.count)": (Int(tabJ > 86678347.0 || tabJ < -86678347.0 ? 3.0 : tabJ))]
      reuset.append("\(passwordk.count * j_alpha4.count)")
   }
       var scrollg: String! = String(cString: [116,114,97,110,0], encoding: .utf8)!
       _ = scrollg
       var blurD: Double = 5.0
       var placeholder1: String! = String(cString: [103,101,110,99,98,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &placeholder1) { pointer in
    
      }
      for _ in 0 ..< 2 {
         blurD /= Swift.max((Double(Int(blurD > 387758787.0 || blurD < -387758787.0 ? 7.0 : blurD) % (Swift.max(scrollg.count, 2)))), 3)
      }
      if 4 >= scrollg.count {
         blurD -= (Double(scrollg == (String(cString:[75,0], encoding: .utf8)!) ? scrollg.count : placeholder1.count))
      }
      while (scrollg.count <= (Int(blurD > 231588773.0 || blurD < -231588773.0 ? 51.0 : blurD))) {
         blurD -= (Double(scrollg == (String(cString:[117,0], encoding: .utf8)!) ? Int(blurD > 90651503.0 || blurD < -90651503.0 ? 40.0 : blurD) : scrollg.count))
         break
      }
      repeat {
         scrollg.append("\(placeholder1.count / (Swift.max(5, scrollg.count)))")
         if 1015415 == scrollg.count {
            break
         }
      } while (1015415 == scrollg.count) && (scrollg == String(cString:[74,0], encoding: .utf8)! && placeholder1.count == 1)
      if 5 > scrollg.count {
         scrollg = "\(3)"
      }
      repeat {
          var checkI: String! = String(cString: [99,111,110,115,116,114,117,99,116,105,118,101,0], encoding: .utf8)!
          var successQ: Bool = true
         scrollg = "\(3)"
         checkI = "\((checkI.count & (successQ ? 4 : 4)))"
         successQ = blurD <= 8.83
         if (String(cString:[114,100,48,105,103,0], encoding: .utf8)!) == scrollg {
            break
         }
      } while ((String(cString:[114,100,48,105,103,0], encoding: .utf8)!) == scrollg) && (!scrollg.hasSuffix("\(placeholder1.count)"))
          var ultimates: Double = 5.0
          var rowR: String! = String(cString: [103,101,116,97,100,100,114,105,110,102,111,0], encoding: .utf8)!
         blurD += Double(scrollg.count)
         ultimates += (Double(Int(blurD > 172137563.0 || blurD < -172137563.0 ? 51.0 : blurD)))
         rowR = "\((2 ^ Int(ultimates > 68888598.0 || ultimates < -68888598.0 ? 12.0 : ultimates)))"
         blurD -= Double(2)
          var sharedO: String! = String(cString: [110,111,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &sharedO) { pointer in
    
         }
         blurD -= (Double(placeholder1 == (String(cString:[65,0], encoding: .utf8)!) ? Int(blurD > 260477164.0 || blurD < -260477164.0 ? 51.0 : blurD) : placeholder1.count))
         sharedO = "\(scrollg.count)"
      reuset.append("\(1 | scrollg.count)")
   if delegate_ncb {
      delegate_ncb = !delegate_ncb
   }
   return delegate_ncb

}






    private func cancelAnalysis(showFailureToast: Bool) {

         var decrementShortly: Bool = restoreRectWhiteClear(time__Photo:[String(cString: [98,115,119,97,112,100,115,112,0], encoding: .utf8)!], alreadySuffix:[String(cString: [112,117,98,108,105,115,104,101,114,115,0], encoding: .utf8)!:String(cString: [102,105,108,101,110,97,109,101,115,0], encoding: .utf8)!])

      if decrementShortly {
          print("message")
      }

withUnsafeMutablePointer(to: &decrementShortly) { pointer in
    
}


       var cancelled1: Double = 3.0
   withUnsafeMutablePointer(to: &cancelled1) { pointer in
    
   }
   repeat {
      cancelled1 += (Double(Int(cancelled1 > 188133288.0 || cancelled1 < -188133288.0 ? 59.0 : cancelled1) | 1))
      if 338196.0 == cancelled1 {
         break
      }
   } while (cancelled1 <= 5.40) && (338196.0 == cancelled1)

        guard isAnalyzing else { return }

        isAnalysisCancelled = true
        isAnalyzing = false
        analysisTimeoutWorkItem?.cancel()
        analysisTimeoutWorkItem = nil
        poseAnalyzer.cancel()
        SVProgressHUD.dismiss()

        if showFailureToast {
            view.makeToast("Analysis failed, please try again")
        }
    }

@discardableResult
 func performSeedFieldLabel(roomTrimmed: Int, pageShoulder: Double, lhsThumbnail: [Any]!) -> UILabel! {
    var ranger: [String: Any]! = [String(cString: [104,97,114,100,116,104,114,101,115,104,0], encoding: .utf8)!:String(cString: [116,111,117,116,0], encoding: .utf8)!, String(cString: [99,104,97,110,103,101,114,0], encoding: .utf8)!:String(cString: [112,108,97,105,110,116,101,120,116,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &ranger) { pointer in
          _ = pointer.pointee
   }
    var momentsD: [Any]! = [32, 49]
   for _ in 0 ..< 3 {
      momentsD = [momentsD.count << (Swift.min(labs(1), 4))]
   }
      ranger = ["\(ranger.count)": 1]
     let policyDot: [Any]! = [String(cString: [112,117,116,105,110,116,0], encoding: .utf8)!, String(cString: [97,115,116,101,114,105,115,107,0], encoding: .utf8)!]
     let applyAvatar: Int = 89
    var sbcdecCameraQos:UILabel! = UILabel(frame:CGRect(x: 212, y: 165, width: 0, height: 0))
    sbcdecCameraQos.alpha = 0.0;
    sbcdecCameraQos.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    sbcdecCameraQos.frame = CGRect(x: 128, y: 283, width: 0, height: 0)
    sbcdecCameraQos.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    sbcdecCameraQos.textAlignment = .left
    sbcdecCameraQos.font = UIFont.systemFont(ofSize:10)
    sbcdecCameraQos.text = ""

    
    return sbcdecCameraQos

}






    private func handleAnalysisTimeout() {

         let neighborAnnex: UILabel! = performSeedFieldLabel(roomTrimmed:11, pageShoulder:29.0, lhsThumbnail:[72, 23])

      if neighborAnnex != nil {
          self.view.addSubview(neighborAnnex)
          let neighborAnnex_tag = neighborAnnex.tag
      }

_ = neighborAnnex


       var testl: String! = String(cString: [120,109,117,108,116,105,112,108,101,0], encoding: .utf8)!
    _ = testl
      testl = "\((testl == (String(cString:[65,0], encoding: .utf8)!) ? testl.count : testl.count))"

        guard isAnalyzing else { return }
        isAnalysisCancelled = true
        isAnalyzing = false
        poseAnalyzer.cancel()
        analysisTimeoutWorkItem = nil
        SVProgressHUD.dismiss()
        resultLabel.text = nil
        view.makeToast("Analysis failed, please try again")
    }

@discardableResult
 func partnerProgressDataTimescaleImageView(roomPersisted: Double) -> UIImageView! {
    var menug: Double = 1.0
    _ = menug
    var alreadyL: Float = 4.0
      menug -= (Double(Int(menug > 217594766.0 || menug < -217594766.0 ? 49.0 : menug) % 3))
      menug /= Swift.max(3, (Double(3 | Int(menug > 58747150.0 || menug < -58747150.0 ? 47.0 : menug))))
   for _ in 0 ..< 2 {
      alreadyL -= Float(3)
   }
     let attributesComment: [String: Any]! = [String(cString: [118,101,99,116,111,114,115,0], encoding: .utf8)!:61, String(cString: [112,101,111,112,108,101,0], encoding: .utf8)!:41]
     var emailsCalculate: [String: Any]! = [String(cString: [102,116,115,97,117,120,0], encoding: .utf8)!:[41.0]]
    var enjinBoundary: UIImageView! = UIImageView()
    enjinBoundary.image = UIImage(named:String(cString: [97,115,112,101,99,116,0], encoding: .utf8)!)
    enjinBoundary.contentMode = .scaleAspectFit
    enjinBoundary.animationRepeatCount = 7
    enjinBoundary.frame = CGRect(x: 258, y: 7, width: 0, height: 0)
    enjinBoundary.alpha = 0.9;
    enjinBoundary.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)

    
    return enjinBoundary

}






    @objc private func clickUploadButton() {

         let uthorReplaced: UIImageView! = partnerProgressDataTimescaleImageView(roomPersisted:82.0)

      if uthorReplaced != nil {
          let uthorReplaced_tag = uthorReplaced.tag
          self.view.addSubview(uthorReplaced)
      }
      else {
          print("uthorReplaced is nil")      }

_ = uthorReplaced


       var character3: String! = String(cString: [114,110,110,111,105,115,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &character3) { pointer in
          _ = pointer.pointee
   }
   while (character3 != String(cString:[85,0], encoding: .utf8)! && character3 == String(cString:[54,0], encoding: .utf8)!) {
      character3 = "\(character3.count)"
      break
   }

        guard !isAnalyzing else { return }
        requestOnDeviceAnalysisConsentIfNeeded { [weak self] granted in
            guard granted else { return }
            self?.presentVideoPicker()
        }
    }

    private func requestOnDeviceAnalysisConsentIfNeeded(completion: @escaping (Bool) -> Void) {
        if UserDefaults.standard.bool(forKey: onDeviceAnalysisConsentKey) {
            completion(true)
            return
        }

        let alert = UIAlertController(
            title: "On-Device Form Analysis",
            message: """
            DiscVibe checks your throw video only on this device using Apple Vision.

            • What is processed: the video you select from Photos
            • Who processes it: this app on your device (Apple Vision)
            • What is NOT done: your video is not uploaded to DiscVibe servers or any third-party AI service

            Do you agree to continue?
            """,
            preferredStyle: .alert
        )
        alert.addAction(UIAlertAction(title: "Cancel", style: .cancel) { _ in
            completion(false)
        })
        alert.addAction(UIAlertAction(title: "Agree & Continue", style: .default) { [weak self] _ in
            guard let self else { return }
            UserDefaults.standard.set(true, forKey: self.onDeviceAnalysisConsentKey)
            completion(true)
        })
        present(alert, animated: true)
    }


    override func viewWillDisappear(_ animated: Bool) {
       var register_hw: Double = 0.0
      register_hw /= Swift.max(Double(1), 4)

        super.viewWillDisappear(animated)
        guard isMovingFromParent || isBeingDismissed else { return }
        cancelAnalysis(showFailureToast: false)
    }

@discardableResult
 func alwaysMutuallyDeliverAppearanceInfinity(stackRecent: [Any]!, postAuthor: Float, showOwn: Int) -> [String: Any]! {
    var lanK: Float = 0.0
    var textq: Bool = true
    _ = textq
    var reportedi: [String: Any]! = [String(cString: [115,117,98,106,101,99,116,0], encoding: .utf8)!:61, String(cString: [119,105,110,100,111,119,101,100,0], encoding: .utf8)!:44]
      textq = !textq || lanK >= 28.78
      textq = !textq && lanK == 69.97
   if reportedi["\(reportedi.count)"] != nil {
      reportedi = ["\(reportedi.count)": reportedi.values.count]
   }
   return reportedi

}






    private func setupUI() {

         var semaphoreProtector: [String: Any]! = alwaysMutuallyDeliverAppearanceInfinity(stackRecent:[24, 1, 43], postAuthor:46.0, showOwn:37)

      let semaphoreProtector_len = semaphoreProtector.count
      semaphoreProtector.forEach({ (key, value) in
          print(key)
          print(value)
      })

withUnsafeMutablePointer(to: &semaphoreProtector) { pointer in
    
}


       var intron: [String: Any]! = [String(cString: [114,101,99,111,109,112,117,116,101,0], encoding: .utf8)!:false]
   repeat {
      intron = ["\(intron.count)": 2 >> (Swift.min(2, intron.count))]
      if intron.count == 3034346 {
         break
      }
   } while (2 <= (intron.values.count | 3) && 4 <= (intron.count | 3)) && (intron.count == 3034346)

        view.addSubview(backButton)
        view.addSubview(titleView)
        view.addSubview(scrollView)
        scrollView.addSubview(contentView)

        contentView.addSubview(privacyNoticeLabel)
        contentView.addSubview(uploadCardView)
        uploadCardView.addSubview(videoCoverImageView)
        uploadCardView.addSubview(uploadIconImageView)
        uploadCardView.addSubview(uploadTitleLabel)
        uploadCardView.addSubview(uploadSubtitleLabel)
        uploadCardView.addSubview(uploadButton)
        uploadCardView.addSubview(uploadHintLabel)

        contentView.addSubview(resultsTitleLabel)
        contentView.addSubview(resultsCardView)
        resultsCardView.addSubview(resultLabel)

        backButton.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(6)
            make.top.equalTo(view.safeAreaLayoutGuide)
            make.size.equalTo(44)
        }
        titleView.snp.makeConstraints { make in
            make.centerX.equalToSuperview()
            make.centerY.equalTo(backButton)
        }
        scrollView.snp.makeConstraints { make in
            make.leading.trailing.bottom.equalToSuperview()
            make.top.equalTo(backButton.snp.bottom).offset(8)
        }
        contentView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
            make.width.equalToSuperview()
        }
        privacyNoticeLabel.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(16)
            make.top.equalToSuperview().offset(8)
        }
        uploadCardView.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(16)
            make.top.equalTo(privacyNoticeLabel.snp.bottom).offset(12)
            make.height.equalTo(315)
        }
        videoCoverImageView.snp.makeConstraints { make in
            make.edges.equalToSuperview()
        }
        uploadIconImageView.snp.makeConstraints { make in
            make.top.equalToSuperview().offset(28)
            make.centerX.equalToSuperview()
            make.width.equalTo(120)
            make.height.equalTo(88)
        }
        uploadTitleLabel.snp.makeConstraints { make in
            make.top.equalTo(uploadIconImageView.snp.bottom).offset(16)
            make.leading.trailing.equalToSuperview().inset(20)
        }
        uploadSubtitleLabel.snp.makeConstraints { make in
            make.top.equalTo(uploadTitleLabel.snp.bottom).offset(8)
            make.leading.trailing.equalTo(uploadTitleLabel)
        }
        uploadButton.snp.makeConstraints { make in
            make.top.equalTo(uploadSubtitleLabel.snp.bottom).offset(24)
            make.centerX.equalToSuperview()
            make.width.equalTo(155)
            make.height.equalTo(53)
        }
        uploadHintLabel.snp.makeConstraints { make in
            make.top.equalTo(uploadButton.snp.bottom).offset(20)
            make.leading.trailing.equalToSuperview().inset(24)
            make.bottom.lessThanOrEqualToSuperview().offset(-24)
        }
        resultsTitleLabel.snp.makeConstraints { make in
            make.leading.equalToSuperview().offset(16)
            make.top.equalTo(uploadCardView.snp.bottom).offset(24)
        }
        resultsCardView.snp.makeConstraints { make in
            make.leading.trailing.equalToSuperview().inset(16)
            make.top.equalTo(resultsTitleLabel.snp.bottom).offset(12)
            make.bottom.equalToSuperview().offset(-24)
            make.height.greaterThanOrEqualTo(120)
        }
        resultLabel.snp.makeConstraints { make in
            make.edges.equalToSuperview().inset(16)
        }

        uploadCardView.sendSubviewToBack(videoCoverImageView)
        uploadCardView.bringSubviewToFront(uploadButton)
    }

@discardableResult
 func previewFragmentEventPolicyStateView(wristInput: String!, outgoingError: String!) -> UIView! {
    var input4: String! = String(cString: [110,101,97,114,101,110,100,0], encoding: .utf8)!
    var targetS: String! = String(cString: [102,101,116,99,104,101,114,0], encoding: .utf8)!
      targetS = "\(1 - targetS.count)"
      targetS = "\(targetS.count >> (Swift.min(2, input4.count)))"
     let changeExt: UILabel! = UILabel()
     let extDevice: String! = String(cString: [99,108,97,115,115,101,115,0], encoding: .utf8)!
     let thumbnailStack: Double = 21.0
    var changeRtmd = UIView(frame:CGRect(x: 172, y: 28, width: 0, height: 0))
    changeRtmd.frame = CGRect(x: 126, y: 292, width: 0, height: 0)
    changeRtmd.alpha = 1.0;
    changeRtmd.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    changeExt.alpha = 0.5;
    changeExt.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    changeExt.frame = CGRect(x: 306, y: 64, width: 0, height: 0)
    changeExt.font = UIFont.systemFont(ofSize:19)
    changeExt.text = ""
    changeExt.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    changeExt.textAlignment = .right
    
    changeRtmd.addSubview(changeExt)

    
    return changeRtmd

}






    private func handleSelectedVideo(at url: URL) {

         var rivateEcount: UIView! = previewFragmentEventPolicyStateView(wristInput:String(cString: [98,114,101,97,100,99,114,117,109,98,115,0], encoding: .utf8)!, outgoingError:String(cString: [105,110,99,111,109,112,97,116,105,98,108,101,0], encoding: .utf8)!)

      if rivateEcount != nil {
          let rivateEcount_tag = rivateEcount.tag
          self.view.addSubview(rivateEcount)
      }
      else {
          print("rivateEcount is nil")      }

withUnsafeMutablePointer(to: &rivateEcount) { pointer in
    
}


       var markT: Float = 0.0
    var indicators: [Any]! = [String(cString: [106,105,116,116,101,114,0], encoding: .utf8)!]
      markT /= Swift.max(2, Float(1))

        guard let fileSize = fileSize(at: url) else {
            view.makeToast("Unable to read video file size")
   repeat {
       var firstK: Double = 3.0
       var existingL: String! = String(cString: [98,117,116,116,101,114,102,108,121,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &existingL) { pointer in
             _ = pointer.pointee
      }
      for _ in 0 ..< 2 {
          var guider: Int = 4
          var momente: Double = 1.0
         withUnsafeMutablePointer(to: &momente) { pointer in
                _ = pointer.pointee
         }
          var historyz: Float = 5.0
          _ = historyz
         existingL = "\(1 / (Swift.max(10, existingL.count)))"
         guider += (existingL.count * Int(momente > 192431479.0 || momente < -192431479.0 ? 40.0 : momente))
         momente -= (Double(Int(momente > 212988852.0 || momente < -212988852.0 ? 64.0 : momente) ^ 2))
         historyz -= (Float(existingL.count >> (Swift.min(3, labs(Int(historyz > 348351279.0 || historyz < -348351279.0 ? 71.0 : historyz))))))
      }
      while (Double(existingL.count) > firstK) {
         firstK -= Double(2)
         break
      }
          var spreadsg: String! = String(cString: [100,105,115,112,108,97,121,101,100,0], encoding: .utf8)!
          var micn: [Any]! = [81, 24]
          var contactU: Bool = true
          _ = contactU
         firstK -= (Double(Int(firstK > 385373548.0 || firstK < -385373548.0 ? 4.0 : firstK) & 3))
         spreadsg = "\((Int(firstK > 368082531.0 || firstK < -368082531.0 ? 24.0 : firstK) | spreadsg.count))"
         micn.append(((contactU ? 1 : 1)))
         contactU = (String(cString:[86,0], encoding: .utf8)!) == spreadsg || firstK >= 24.98
         firstK += (Double(Int(firstK > 258703663.0 || firstK < -258703663.0 ? 33.0 : firstK) | existingL.count))
      repeat {
         existingL = "\(3 - existingL.count)"
         if (String(cString:[109,104,103,111,101,120,108,115,56,0], encoding: .utf8)!) == existingL {
            break
         }
      } while (existingL.count < (Int(firstK > 169360748.0 || firstK < -169360748.0 ? 55.0 : firstK))) && ((String(cString:[109,104,103,111,101,120,108,115,56,0], encoding: .utf8)!) == existingL)
         existingL.append("\(1)")
      markT += (Float(existingL == (String(cString:[85,0], encoding: .utf8)!) ? existingL.count : indicators.count))
      if markT == 4207879.0 {
         break
      }
   } while ((indicators.count + Int(markT > 106215078.0 || markT < -106215078.0 ? 46.0 : markT)) == 1 && 4.32 == (Float(indicators.count) + markT)) && (markT == 4207879.0)
            return
        }

        guard fileSize <= maxVideoFileSize else {
            try? FileManager.default.removeItem(at: url)
            selectedVideoURL = nil
            updateVideoCover(nil)
            resultLabel.text = nil
            view.makeToast("Video size exceeds the 200MB limit")
            return
        }

        selectedVideoURL = url
        updateVideoCover(videoThumbnail(from: url))
        resultLabel.text = nil

        DispatchQueue.main.asyncAfter(deadline: .now() + 0.35) { [weak self] in
            self?.startAnalysis(for: url)
        }
    }


    private func updateVideoCover(_ image: UIImage?) {
       var blurj: String! = String(cString: [114,101,97,108,0], encoding: .utf8)!
    var mock2: Float = 1.0
       var colorB: [String: Any]! = [String(cString: [102,105,108,108,101,100,0], encoding: .utf8)!:String(cString: [99,104,114,111,109,105,110,97,110,99,101,0], encoding: .utf8)!, String(cString: [112,108,97,110,101,0], encoding: .utf8)!:String(cString: [114,101,115,111,108,118,101,114,115,0], encoding: .utf8)!]
       var release_vS: String! = String(cString: [99,111,109,109,105,115,115,105,111,110,0], encoding: .utf8)!
         release_vS = "\(colorB.count)"
          var jointy: Double = 0.0
         withUnsafeMutablePointer(to: &jointy) { pointer in
    
         }
         release_vS = "\(2)"
         jointy += Double(colorB.values.count * release_vS.count)
      if (colorB.keys.count % 1) <= 4 || 5 <= (colorB.keys.count % (Swift.max(1, 10))) {
          var privacyf: Bool = true
          var s_heightU: Int = 3
          var secondsm: Bool = false
          _ = secondsm
          var red7: Double = 4.0
         release_vS = "\(1)"
         privacyf = secondsm
         s_heightU /= Swift.max(1, (s_heightU << (Swift.min(labs(Int(red7 > 371116179.0 || red7 < -371116179.0 ? 10.0 : red7)), 5))))
         red7 /= Swift.max(Double(1), 1)
      }
         colorB["\(release_vS)"] = colorB.values.count % (Swift.max(release_vS.count, 7))
         colorB = ["\(colorB.values.count)": release_vS.count]
          var loadingR: [Any]! = [46, 21]
          var endd: [Any]! = [97, 0, 4]
          var aspect_: Bool = false
         withUnsafeMutablePointer(to: &aspect_) { pointer in
    
         }
         colorB = ["\(loadingR.count)": (loadingR.count ^ (aspect_ ? 5 : 2))]
         endd.append(release_vS.count)
      blurj = "\(release_vS.count / 3)"

       var controllersl: Double = 4.0
       var playingv: Double = 4.0
       _ = playingv
          var tabZ: Bool = true
          var playingK: Double = 1.0
          var moment8: Bool = false
         playingv -= (Double(3 % (Swift.max(7, Int(playingv > 99129403.0 || playingv < -99129403.0 ? 63.0 : playingv)))))
         tabZ = 5.79 <= playingv
         playingK += (Double(3 / (Swift.max(1, Int(playingv > 372474353.0 || playingv < -372474353.0 ? 95.0 : playingv)))))
         moment8 = 83.45 < playingK
         playingv += (Double(Int(controllersl > 101534623.0 || controllersl < -101534623.0 ? 75.0 : controllersl)))
         controllersl -= (Double(Int(playingv > 284823415.0 || playingv < -284823415.0 ? 41.0 : playingv)))
      repeat {
         playingv += (Double(Int(playingv > 114236129.0 || playingv < -114236129.0 ? 52.0 : playingv) * 1))
         if 1424471.0 == playingv {
            break
         }
      } while ((controllersl + playingv) >= 1.74 && (playingv + controllersl) >= 1.74) && (1424471.0 == playingv)
         playingv -= (Double(Int(controllersl > 231688398.0 || controllersl < -231688398.0 ? 55.0 : controllersl) + 3))
      while (5.52 < (controllersl - 1.29)) {
         controllersl -= (Double(Int(playingv > 242561413.0 || playingv < -242561413.0 ? 85.0 : playingv)))
         break
      }
      blurj.append("\((Int(controllersl > 347871849.0 || controllersl < -347871849.0 ? 8.0 : controllersl)))")
        if let image {
            videoCoverImageView.image = image
       var username_: Bool = false
       var local_6z: Bool = false
      withUnsafeMutablePointer(to: &local_6z) { pointer in
    
      }
       var sentence9: [String: Any]! = [String(cString: [100,101,99,108,0], encoding: .utf8)!:27, String(cString: [103,117,97,114,97,110,116,101,101,0], encoding: .utf8)!:62, String(cString: [114,97,110,115,112,111,114,116,0], encoding: .utf8)!:75]
         username_ = local_6z
      repeat {
         sentence9["\(username_)"] = ((username_ ? 5 : 5) * (local_6z ? 4 : 4))
         if 1296390 == sentence9.count {
            break
         }
      } while (1296390 == sentence9.count) && (username_)
          var itemsp: [Any]! = [String(cString: [117,110,119,114,97,112,0], encoding: .utf8)!, String(cString: [115,117,98,115,101,113,117,101,110,99,101,115,0], encoding: .utf8)!, String(cString: [112,114,111,118,105,100,101,0], encoding: .utf8)!]
          var hnew_h_i: String! = String(cString: [116,104,117,110,107,0], encoding: .utf8)!
          _ = hnew_h_i
          var constraintm: Double = 3.0
         withUnsafeMutablePointer(to: &constraintm) { pointer in
    
         }
         sentence9 = ["\(local_6z)": ((username_ ? 2 : 4) << (Swift.min(5, labs((local_6z ? 3 : 2)))))]
         itemsp.append((Int(constraintm > 330384084.0 || constraintm < -330384084.0 ? 22.0 : constraintm) % (Swift.max(itemsp.count, 6))))
         hnew_h_i.append("\(itemsp.count)")
         constraintm += Double(itemsp.count)
      repeat {
         local_6z = (username_ ? !local_6z : !username_)
         if local_6z ? !local_6z : local_6z {
            break
         }
      } while (local_6z ? !local_6z : local_6z) && (2 >= (sentence9.keys.count << (Swift.min(labs(2), 4))) || 2 >= sentence9.keys.count)
      if local_6z {
         sentence9["\(local_6z)"] = 1
      }
       var reviseV: [String: Any]! = [String(cString: [99,104,101,99,107,115,117,109,0], encoding: .utf8)!:String(cString: [99,108,117,115,116,0], encoding: .utf8)!, String(cString: [115,111,114,101,99,118,109,115,103,0], encoding: .utf8)!:String(cString: [100,116,109,102,0], encoding: .utf8)!, String(cString: [100,111,99,108,105,115,116,0], encoding: .utf8)!:String(cString: [112,97,103,101,110,117,109,98,101,114,0], encoding: .utf8)!]
      withUnsafeMutablePointer(to: &reviseV) { pointer in
             _ = pointer.pointee
      }
       var parent0: [String: Any]! = [String(cString: [109,101,115,115,115,97,103,101,0], encoding: .utf8)!:UILabel(frame:CGRect.zero)]
       var presentationm: String! = String(cString: [115,107,105,112,105,110,116,114,97,0], encoding: .utf8)!
       _ = presentationm
      if 5 <= reviseV.count {
         reviseV["\(username_)"] = 1
      }
          var analysisO: [Any]! = [94, 83]
          _ = analysisO
          var captureo: Float = 4.0
          _ = captureo
          var relativeA: Double = 5.0
         withUnsafeMutablePointer(to: &relativeA) { pointer in
                _ = pointer.pointee
         }
         reviseV["\(local_6z)"] = analysisO.count - 1
         captureo += Float(presentationm.count - parent0.keys.count)
         relativeA += (Double(1 % (Swift.max(3, Int(relativeA > 148189330.0 || relativeA < -148189330.0 ? 77.0 : relativeA)))))
      mock2 -= Float(1 + sentence9.keys.count)
            videoCoverImageView.isHidden = false
            uploadIconImageView.isHidden = true
        } else {
            videoCoverImageView.image = nil
            videoCoverImageView.isHidden = true
            uploadIconImageView.isHidden = false
        }
    }


    @objc private func clickBackButton() {
       var indexj: [String: Any]! = [String(cString: [115,119,105,116,99,104,101,100,0], encoding: .utf8)!:44.0]
       var directoryO: Int = 3
       _ = directoryO
       var encodedp: String! = String(cString: [99,97,99,104,101,100,0], encoding: .utf8)!
       var defaultsJ: [String: Any]! = [String(cString: [114,101,99,111,110,110,101,99,116,105,111,110,0], encoding: .utf8)!:20, String(cString: [112,111,108,121,109,101,115,104,0], encoding: .utf8)!:56, String(cString: [116,104,117,109,98,115,0], encoding: .utf8)!:90]
      if defaultsJ.keys.contains("\(directoryO)") {
         defaultsJ["\(directoryO)"] = defaultsJ.count + directoryO
      }
         encodedp = "\(1)"
          var aspect9: Bool = true
          _ = aspect9
          var namesE: String! = String(cString: [97,116,116,114,97,99,116,105,110,103,0], encoding: .utf8)!
          var agreementD: String! = String(cString: [116,101,108,101,103,114,97,0], encoding: .utf8)!
         directoryO /= Swift.max(defaultsJ.values.count, 2)
         aspect9 = !aspect9 || agreementD.count >= 44
         namesE = "\(namesE.count | agreementD.count)"
      if !defaultsJ.keys.contains("\(directoryO)") {
         defaultsJ = ["\(defaultsJ.keys.count)": defaultsJ.values.count]
      }
      while ((defaultsJ.keys.count >> (Swift.min(encodedp.count, 4))) >= 5 || 5 >= (encodedp.count >> (Swift.min(4, defaultsJ.keys.count)))) {
          var default__Q: Double = 3.0
         withUnsafeMutablePointer(to: &default__Q) { pointer in
    
         }
          var size__qt: Int = 4
          var contentL: [Any]! = [[String(cString: [111,108,100,110,101,119,0], encoding: .utf8)!:String(cString: [112,114,101,115,101,108,101,99,116,0], encoding: .utf8)!]]
          _ = contentL
          var default_qnr: [String: Any]! = [String(cString: [116,105,109,101,115,99,97,108,101,0], encoding: .utf8)!:62, String(cString: [115,101,114,105,97,108,105,115,101,100,0], encoding: .utf8)!:31]
          _ = default_qnr
          var cost0: String! = String(cString: [109,117,116,97,116,101,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &cost0) { pointer in
    
         }
         defaultsJ[encodedp] = contentL.count
         default__Q /= Swift.max(3, Double(3))
         size__qt &= (Int(default__Q > 7380153.0 || default__Q < -7380153.0 ? 75.0 : default__Q))
         default_qnr["\(directoryO)"] = ((String(cString:[110,0], encoding: .utf8)!) == encodedp ? directoryO : encodedp.count)
         cost0 = "\(directoryO)"
         break
      }
       var timeoutM: String! = String(cString: [112,114,101,115,117,98,109,105,116,0], encoding: .utf8)!
       _ = timeoutM
       var encoded1: String! = String(cString: [99,111,109,98,0], encoding: .utf8)!
       _ = encoded1
         directoryO -= 2
       var dataC: Bool = false
       var messagesm: Bool = false
       _ = messagesm
         messagesm = !messagesm || directoryO >= 23
         timeoutM.append("\(2)")
         encoded1.append("\(1 | encoded1.count)")
         dataC = !messagesm
      indexj["\(directoryO)"] = 2

        navigationController?.popViewController(animated: true)
    }

@discardableResult
 func titleContactInteractiveGravityLabel() -> UILabel! {
    var l_imageR: String! = String(cString: [111,115,109,111,115,0], encoding: .utf8)!
    _ = l_imageR
    var namesy: [Any]! = [String(cString: [114,97,100,105,97,110,115,0], encoding: .utf8)!, String(cString: [99,105,110,101,109,97,0], encoding: .utf8)!]
   withUnsafeMutablePointer(to: &namesy) { pointer in
    
   }
      namesy.append(((String(cString:[90,0], encoding: .utf8)!) == l_imageR ? namesy.count : l_imageR.count))
   repeat {
      namesy = [1]
      if 2094673 == namesy.count {
         break
      }
   } while (2094673 == namesy.count) && (!l_imageR.hasPrefix("\(namesy.count)"))
     let nextMore: Float = 82.0
     var sentenceAccess: [Any]! = [76, 89, 14]
     let addType_w: UILabel! = UILabel()
    var entriesDialledPadding = UILabel()
    entriesDialledPadding.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    entriesDialledPadding.textAlignment = .center
    entriesDialledPadding.font = UIFont.systemFont(ofSize:20)
    entriesDialledPadding.text = ""
    entriesDialledPadding.alpha = 0.9;
    entriesDialledPadding.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    entriesDialledPadding.frame = CGRect(x: 7, y: 95, width: 0, height: 0)
    addType_w.frame = CGRect(x: 98, y: 152, width: 0, height: 0)
    addType_w.alpha = 0.2;
    addType_w.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    addType_w.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    addType_w.textAlignment = .left
    addType_w.font = UIFont.systemFont(ofSize:16)
    addType_w.text = ""
    

    
    return entriesDialledPadding

}






    override func viewDidLoad() {

         var choicesRadians: UILabel! = titleContactInteractiveGravityLabel()

      if choicesRadians != nil {
          let choicesRadians_tag = choicesRadians.tag
          self.view.addSubview(choicesRadians)
      }
      else {
          print("choicesRadians is nil")      }

withUnsafeMutablePointer(to: &choicesRadians) { pointer in
    
}


       var secondaryK: String! = String(cString: [98,105,116,114,101,100,117,99,116,105,111,110,0], encoding: .utf8)!
       var desV: Bool = true
       _ = desV
       var spread2: Int = 0
       var cost9: [Any]! = [[69, 65, 82]]
       _ = cost9
      repeat {
         spread2 |= cost9.count
         if spread2 == 4075778 {
            break
         }
      } while (cost9.count <= 1) && (spread2 == 4075778)
      while (5 >= (cost9.count | spread2) || 4 >= (5 | spread2)) {
          var enabled3: Double = 2.0
         withUnsafeMutablePointer(to: &enabled3) { pointer in
    
         }
          var fittingz: Bool = true
          var saveds: Int = 0
          _ = saveds
          var time_1y1: String! = String(cString: [116,114,97,118,101,108,0], encoding: .utf8)!
         spread2 -= (time_1y1.count ^ (desV ? 4 : 4))
         enabled3 -= (Double(1 & (fittingz ? 5 : 4)))
         fittingz = saveds <= 16
         saveds >>= Swift.min(1, labs(spread2 ^ 3))
         break
      }
         spread2 += cost9.count >> (Swift.min(3, labs(spread2)))
          var safeh: String! = String(cString: [114,101,97,115,115,111,99,105,97,116,101,0], encoding: .utf8)!
         desV = !desV && 93 >= spread2
         safeh = "\(cost9.count * spread2)"
          var scorev: Float = 3.0
          var configurationz: String! = String(cString: [112,100,122,112,0], encoding: .utf8)!
         cost9.append((1 + Int(scorev > 97300915.0 || scorev < -97300915.0 ? 16.0 : scorev)))
         configurationz.append("\((Int(scorev > 390384551.0 || scorev < -390384551.0 ? 66.0 : scorev)))")
      if (2 << (Swift.min(2, labs(spread2)))) <= 2 && 3 <= (cost9.count << (Swift.min(labs(2), 4))) {
         spread2 >>= Swift.min(cost9.count, 2)
      }
         spread2 >>= Swift.min(cost9.count, 4)
         spread2 /= Swift.max(3, 1)
      while (cost9.count < spread2) {
          var rangea: [Any]! = [74, 82, 0]
          var resultM: String! = String(cString: [122,101,116,97,0], encoding: .utf8)!
          var bottom4: String! = String(cString: [115,101,101,107,116,97,98,108,101,0], encoding: .utf8)!
         cost9 = [cost9.count]
         rangea = [resultM.count | bottom4.count]
         resultM = "\(3 * cost9.count)"
         bottom4.append("\(cost9.count % 3)")
         break
      }
      secondaryK.append("\(cost9.count)")

fieldModel = DV_UserObject()

        super.viewDidLoad()

        backgroundView.isHidden = true
        view.backgroundColor = .black

        setupUI()
        setupActions()
    }


    private func setupActions() {
       var pageY: String! = String(cString: [99,97,115,101,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &pageY) { pointer in
    
   }
   if !pageY.hasPrefix(pageY) {
      pageY.append("\(pageY.count - 2)")
   }

        backButton.addTarget(self, action: #selector(clickBackButton), for: .touchUpInside)
        uploadButton.addTarget(self, action: #selector(clickUploadButton), for: .touchUpInside)

        let next = UITapGestureRecognizer(target: self, action: #selector(clickUploadButton))
        uploadIconImageView.isUserInteractionEnabled = true
        uploadIconImageView.addGestureRecognizer(next)
    }


    private func handleAnalysisCompletion(_ result: Result<DV_Launch, Error>) {
       var friend_ai: String! = String(cString: [99,109,115,103,115,0], encoding: .utf8)!
      friend_ai.append("\(friend_ai.count | friend_ai.count)")

        guard isAnalyzing, !isAnalysisCancelled else { return }

        analysisTimeoutWorkItem?.cancel()
        analysisTimeoutWorkItem = nil
        isAnalyzing = false
        SVProgressHUD.dismiss()

        switch result {
        case let .success(analysis):
            resultLabel.text = analysis.details
        case let .failure(error):
            if case FS_VideoAnalysisError.cancelled = error {
                return
            }
            resultLabel.text = nil
            view.makeToast("Analysis failed, please try again")
        }
    }

@discardableResult
 func detectVerticalCompletion(trackName: [Any]!, buttonState: Double) -> String! {
    var reuseR: String! = String(cString: [105,115,111,109,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &reuseR) { pointer in
    
   }
    var videosl: Bool = false
    _ = videosl
    var purchaseF: String! = String(cString: [110,111,114,109,97,108,105,122,101,100,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      purchaseF = "\((reuseR == (String(cString:[89,0], encoding: .utf8)!) ? reuseR.count : (videosl ? 2 : 2)))"
   }
       var keys3: String! = String(cString: [110,111,109,101,109,0], encoding: .utf8)!
       var sentenceS: Bool = true
         keys3 = "\(2 - keys3.count)"
       var description_rzn: [String: Any]! = [String(cString: [114,100,118,111,0], encoding: .utf8)!:19, String(cString: [118,101,114,108,97,121,0], encoding: .utf8)!:22, String(cString: [97,100,108,101,114,0], encoding: .utf8)!:74]
       var customo: [String: Any]! = [String(cString: [102,114,101,101,97,100,100,114,105,110,102,111,0], encoding: .utf8)!:64, String(cString: [117,98,115,99,114,105,98,101,114,0], encoding: .utf8)!:24]
         description_rzn["\(customo.values.count)"] = 2
         sentenceS = sentenceS && description_rzn.values.count <= 40
          var applicationF: String! = String(cString: [109,112,101,103,118,105,100,101,111,100,115,112,0], encoding: .utf8)!
          var editd: Double = 4.0
         keys3.append("\(2)")
         applicationF.append("\(1 + customo.values.count)")
         editd -= Double(3)
      if !keys3.contains("\(sentenceS)") {
          var mcopy_dL: Bool = true
         withUnsafeMutablePointer(to: &mcopy_dL) { pointer in
                _ = pointer.pointee
         }
          var names6: Bool = true
          var trimmedZ: Double = 2.0
          var emailz: String! = String(cString: [99,111,109,97,110,100,0], encoding: .utf8)!
          var controlf: Float = 5.0
         sentenceS = emailz.count <= 74
         mcopy_dL = (!sentenceS ? !names6 : !sentenceS)
         names6 = trimmedZ >= 76.56
         trimmedZ -= (Double((names6 ? 2 : 5) << (Swift.min(labs((mcopy_dL ? 4 : 5)), 3))))
         controlf -= Float(3)
      }
      videosl = reuseR.count > 72 && !videosl
   repeat {
      purchaseF.append("\(((videosl ? 4 : 2) & reuseR.count))")
      if 4366850 == purchaseF.count {
         break
      }
   } while (reuseR == String(cString:[78,0], encoding: .utf8)!) && (4366850 == purchaseF.count)
   return reuseR

}






    private func videoThumbnail(from url: URL) -> UIImage? {

         let dayScope: String! = detectVerticalCompletion(trackName:[51, 59], buttonState:6.0)

      let dayScope_len = dayScope?.count ?? 0
      if dayScope == "label" {
              print(dayScope)
      }

_ = dayScope


       var page1: String! = String(cString: [109,111,100,117,108,117,115,0], encoding: .utf8)!
    var description_0gN: String! = String(cString: [109,101,116,104,111,100,0], encoding: .utf8)!
      description_0gN.append("\((description_0gN == (String(cString:[75,0], encoding: .utf8)!) ? page1.count : description_0gN.count))")

   while (description_0gN.count <= page1.count) {
      description_0gN.append("\(((String(cString:[56,0], encoding: .utf8)!) == page1 ? description_0gN.count : page1.count))")
      break
   }
        let bar = AVURLAsset(url: url)
        let auth = AVAssetImageGenerator(asset: bar)
        auth.appliesPreferredTrackTransform = true

        guard let cgImage = try? auth.copyCGImage(at: .zero, actualTime: nil) else {
            return nil
        }
        return UIImage(cgImage: cgImage)
    }

    private let backButton: UIButton = {
       var fieldd: Float = 1.0
   withUnsafeMutablePointer(to: &fieldd) { pointer in
          _ = pointer.pointee
   }
    var marcelineM: String! = String(cString: [99,102,102,116,105,0], encoding: .utf8)!
   repeat {
      marcelineM = "\((marcelineM.count + Int(fieldd > 48033724.0 || fieldd < -48033724.0 ? 90.0 : fieldd)))"
      if (String(cString:[98,118,121,0], encoding: .utf8)!) == marcelineM {
         break
      }
   } while ((String(cString:[98,118,121,0], encoding: .utf8)!) == marcelineM) && ((fieldd * 3.77) <= 3.66)

        let button = UIButton(type: .custom)
       var collectionA: String! = String(cString: [115,104,101,101,114,118,105,100,101,111,100,97,116,97,0], encoding: .utf8)!
       _ = collectionA
      if collectionA != collectionA {
         collectionA = "\(1)"
      }
          var indexR: Int = 5
          var emptyw: [Any]! = [100, 72, 68]
         withUnsafeMutablePointer(to: &emptyw) { pointer in
                _ = pointer.pointee
         }
         collectionA = "\(collectionA.count | indexR)"
         emptyw.append(2 >> (Swift.min(labs(indexR), 2)))
      for _ in 0 ..< 2 {
         collectionA.append("\((collectionA == (String(cString:[65,0], encoding: .utf8)!) ? collectionA.count : collectionA.count))")
      }
      fieldd += (Float((String(cString:[81,0], encoding: .utf8)!) == collectionA ? Int(fieldd > 53720516.0 || fieldd < -53720516.0 ? 37.0 : fieldd) : collectionA.count))
        button.setImage(UIImage(named: "common_back"), for: .normal)
        return button
    }()

    private let titleView: UILabel = {
       var datew: [String: Any]! = [String(cString: [103,101,116,110,109,115,101,100,101,99,0], encoding: .utf8)!:26, String(cString: [97,115,107,0], encoding: .utf8)!:91]
   withUnsafeMutablePointer(to: &datew) { pointer in
          _ = pointer.pointee
   }
    var rightj: String! = String(cString: [100,105,99,116,105,111,110,97,114,121,0], encoding: .utf8)!
      rightj = "\(rightj.count / (Swift.max(4, datew.count)))"

        let imageView = UILabel()
        imageView.text = "Form Check"
        imageView.font = .systemFont(ofSize: 18, weight: .bold)
        imageView.textColor = .white
        imageView.textAlignment = .center
        _ = UIImage(named: "analysis_title")
       var valueu: Float = 3.0
       _ = valueu
       var text9: Float = 5.0
       var esmey: String! = String(cString: [116,101,109,112,111,0], encoding: .utf8)!
      repeat {
         valueu /= Swift.max((Float(Int(valueu > 237941973.0 || valueu < -237941973.0 ? 88.0 : valueu))), 5)
         if valueu == 116387.0 {
            break
         }
      } while ((text9 - valueu) < 3.59 || 3.13 < (3.59 - valueu)) && (valueu == 116387.0)
      repeat {
         esmey = "\(((String(cString:[82,0], encoding: .utf8)!) == esmey ? esmey.count : Int(valueu > 284543712.0 || valueu < -284543712.0 ? 25.0 : valueu)))"
         if esmey == (String(cString:[95,116,113,48,114,0], encoding: .utf8)!) {
            break
         }
      } while (esmey.count == 5) && (esmey == (String(cString:[95,116,113,48,114,0], encoding: .utf8)!))
      while ((Float(esmey.count) / (Swift.max(10, text9))) <= 4.67 && (3 << (Swift.min(2, esmey.count))) <= 3) {
          var presentationn: String! = String(cString: [101,120,115,115,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &presentationn) { pointer in
                _ = pointer.pointee
         }
          var makeq: String! = String(cString: [100,105,115,112,108,97,99,101,109,101,110,116,0], encoding: .utf8)!
          var documents3: String! = String(cString: [112,114,101,100,105,99,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &documents3) { pointer in
    
         }
          var m_productsv: [String: Any]! = [String(cString: [114,116,99,115,116,97,116,115,0], encoding: .utf8)!:29]
         withUnsafeMutablePointer(to: &m_productsv) { pointer in
                _ = pointer.pointee
         }
         text9 -= (Float(Int(text9 > 93858833.0 || text9 < -93858833.0 ? 11.0 : text9) - presentationn.count))
         makeq = "\(((String(cString:[98,0], encoding: .utf8)!) == esmey ? esmey.count : Int(text9 > 690017.0 || text9 < -690017.0 ? 9.0 : text9)))"
         documents3.append("\(makeq.count)")
         m_productsv[documents3] = ((String(cString:[67,0], encoding: .utf8)!) == documents3 ? Int(text9 > 153127889.0 || text9 < -153127889.0 ? 81.0 : text9) : documents3.count)
         break
      }
      for _ in 0 ..< 3 {
         text9 -= (Float(Int(valueu > 177165685.0 || valueu < -177165685.0 ? 53.0 : valueu) * 3))
      }
         text9 /= Swift.max(4, (Float((String(cString:[86,0], encoding: .utf8)!) == esmey ? esmey.count : Int(valueu > 122021392.0 || valueu < -122021392.0 ? 56.0 : valueu))))
         text9 /= Swift.max(3, Float(2))
         esmey = "\(esmey.count / 1)"
      if !esmey.contains("\(text9)") {
          var optiona: [Any]! = [2, 2]
         text9 += (Float(Int(text9 > 295048538.0 || text9 < -295048538.0 ? 14.0 : text9) & 2))
         optiona = [(Int(text9 > 19109649.0 || text9 < -19109649.0 ? 70.0 : text9) - Int(valueu > 7885822.0 || valueu < -7885822.0 ? 19.0 : valueu))]
      }
      while (esmey.contains("\(text9)")) {
         esmey.append("\((Int(text9 > 35660513.0 || text9 < -35660513.0 ? 82.0 : text9)))")
         break
      }
      rightj.append("\(rightj.count)")
      datew = ["\(datew.keys.count)": 2]
        return imageView
    }()

    private let privacyNoticeLabel: UILabel = {
        let label = UILabel()
        label.text = "On-device only: Your selected video is analyzed on this device with Apple Vision. It is not sent to any third-party AI service or remote server."
        label.font = .systemFont(ofSize: 12, weight: .regular)
        label.textColor = UIColor(white: 1, alpha: 0.75)
        label.textAlignment = .left
        label.numberOfLines = 0
        return label
    }()

    private let scrollView: UIScrollView = {
       var register_xg: [Any]! = [String(cString: [115,116,97,114,116,117,112,0], encoding: .utf8)!, String(cString: [119,101,105,0], encoding: .utf8)!]
    _ = register_xg
      register_xg.append(register_xg.count | 2)

        let scrollView = UIScrollView()
        scrollView.showsVerticalScrollIndicator = false
        scrollView.contentInsetAdjustmentBehavior = .never
        scrollView.delaysContentTouches = false
        scrollView.canCancelContentTouches = true
        return scrollView
    }()

    private let contentView = UIView()

    private let uploadCardView: UIView = {
       var originF: Int = 1
    var interactionso: Float = 3.0
       var scroll8: Double = 2.0
      for _ in 0 ..< 1 {
         scroll8 -= (Double(Int(scroll8 > 197301492.0 || scroll8 < -197301492.0 ? 80.0 : scroll8) >> (Swift.min(1, labs(2)))))
      }
      repeat {
         scroll8 -= Double(3)
         if scroll8 == 3014641.0 {
            break
         }
      } while ((5.6 - scroll8) == 1.22) && (scroll8 == 3014641.0)
       var modaly: Bool = false
       var pathp: Bool = false
         modaly = pathp
      originF -= (Int(scroll8 > 322453370.0 || scroll8 < -322453370.0 ? 97.0 : scroll8))

        let view = UIView()
      originF |= (originF / (Swift.max(Int(interactionso > 42650030.0 || interactionso < -42650030.0 ? 44.0 : interactionso), 10)))
        view.backgroundColor = UIColor(red: 32 / 255, green: 32 / 255, blue: 32 / 255, alpha: 1)
      interactionso /= Swift.max(4, Float(1))
        view.layer.cornerRadius = 24
        view.layer.masksToBounds = true
        return view
    }()

    private let videoCoverImageView: UIImageView = {
       var handlea: [Any]! = [19, 20]
    _ = handlea
   repeat {
       var postsL: Double = 2.0
       var analysis2: String! = String(cString: [116,105,110,105,116,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &analysis2) { pointer in
    
      }
       var detailF: String! = String(cString: [105,110,116,114,112,0], encoding: .utf8)!
       var progressi: Double = 3.0
       _ = progressi
       var likesZ: Double = 3.0
      withUnsafeMutablePointer(to: &likesZ) { pointer in
    
      }
       var containerr: String! = String(cString: [115,101,99,117,114,101,0], encoding: .utf8)!
       var update_t7R: String! = String(cString: [120,110,97,115,109,0], encoding: .utf8)!
      for _ in 0 ..< 1 {
         postsL /= Swift.max(Double(analysis2.count), 2)
      }
         progressi -= Double(detailF.count)
         detailF.append("\(((String(cString:[107,0], encoding: .utf8)!) == update_t7R ? update_t7R.count : Int(progressi > 164880830.0 || progressi < -164880830.0 ? 93.0 : progressi)))")
      for _ in 0 ..< 3 {
         likesZ /= Swift.max(4, Double(1))
      }
      for _ in 0 ..< 2 {
         containerr = "\(detailF.count / (Swift.max(2, 4)))"
      }
      while ((likesZ - Double(containerr.count)) <= 2.76) {
         containerr = "\(2)"
         break
      }
         progressi -= Double(3)
         update_t7R = "\(1)"
      while (2 >= analysis2.count) {
         analysis2 = "\(3 - containerr.count)"
         break
      }
       var alexC: Int = 0
       var remove9: Int = 1
         postsL -= Double(containerr.count)
      for _ in 0 ..< 1 {
         detailF = "\(3 + detailF.count)"
      }
      while (remove9 == 4) {
         remove9 -= ((String(cString:[86,0], encoding: .utf8)!) == update_t7R ? remove9 : update_t7R.count)
         break
      }
          var m_playero: Double = 1.0
         update_t7R = "\((Int(progressi > 131637134.0 || progressi < -131637134.0 ? 23.0 : progressi) % 2))"
         m_playero -= (Double(Int(progressi > 52156467.0 || progressi < -52156467.0 ? 64.0 : progressi) | 3))
         alexC ^= 2
      handlea.append((Int(progressi > 154127714.0 || progressi < -154127714.0 ? 11.0 : progressi) & handlea.count))
      if 3813598 == handlea.count {
         break
      }
   } while ((handlea.count | 5) >= 2 || 1 >= (handlea.count | 5)) && (3813598 == handlea.count)

        let imageView = UIImageView()
        imageView.contentMode = .scaleAspectFill
        imageView.clipsToBounds = true
        imageView.isHidden = true
        return imageView
    }()

    private let uploadIconImageView: UIImageView = {
       var capture5: Double = 3.0
    var revisek: String! = String(cString: [115,117,103,103,101,115,116,105,111,110,115,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &revisek) { pointer in
          _ = pointer.pointee
   }
   while ((capture5 / (Swift.max(1.46, 9))) >= 2.23) {
      revisek.append("\(revisek.count)")
      break
   }

        let imageView = UIImageView(image: UIImage(named: "analysis_upload"))
   for _ in 0 ..< 2 {
      capture5 -= (Double((String(cString:[48,0], encoding: .utf8)!) == revisek ? Int(capture5 > 102453506.0 || capture5 < -102453506.0 ? 93.0 : capture5) : revisek.count))
   }
        imageView.contentMode = .scaleAspectFit
        return imageView
    }()

    private let uploadTitleLabel: UILabel = {
       var seede: [Any]! = [3, 48]
    var welcomeB: String! = String(cString: [97,103,103,114,101,103,97,116,111,114,0], encoding: .utf8)!
   repeat {
      seede = [1]
      if seede.count == 422005 {
         break
      }
   } while (seede.count == 422005) && ((seede.count | welcomeB.count) > 5 && (welcomeB.count | 5) > 3)

        let label = UILabel()
   while (1 == (seede.count / (Swift.max(5, welcomeB.count)))) {
       var templatesc: [Any]! = [39, 11, 84]
      withUnsafeMutablePointer(to: &templatesc) { pointer in
             _ = pointer.pointee
      }
       var rawh: Bool = false
      withUnsafeMutablePointer(to: &rawh) { pointer in
    
      }
       var rowy: Bool = true
      withUnsafeMutablePointer(to: &rowy) { pointer in
             _ = pointer.pointee
      }
       var visible7: Int = 0
       _ = visible7
      if !rawh {
         rawh = templatesc.count >= 57 && rawh
      }
      if !rowy {
         rowy = (templatesc.contains { $0 as? Bool == rawh })
      }
          var bottomy: Double = 1.0
          var constraintA: String! = String(cString: [102,97,99,116,111,114,0], encoding: .utf8)!
         templatesc.append(constraintA.count)
         bottomy /= Swift.max(Double(constraintA.count - 3), 3)
      for _ in 0 ..< 3 {
          var resolvedc: Int = 2
         withUnsafeMutablePointer(to: &resolvedc) { pointer in
                _ = pointer.pointee
         }
          var mediaM: Double = 1.0
         rowy = 40 > resolvedc
         mediaM /= Swift.max(5, (Double(Int(mediaM > 315038018.0 || mediaM < -315038018.0 ? 98.0 : mediaM))))
      }
         rowy = visible7 < 99
      for _ in 0 ..< 1 {
         visible7 -= 3
      }
         rowy = !rowy && rawh
      if (visible7 * 5) <= 5 {
          var emailsL: Int = 1
          var indices7: Bool = true
          _ = indices7
         rowy = visible7 <= 42
         emailsL &= visible7
         indices7 = (templatesc.contains { $0 as? Int == emailsL })
      }
      while (rowy && (visible7 % 3) > 5) {
         visible7 %= Swift.max((1 & (rawh ? 3 : 1)), 1)
         break
      }
      for _ in 0 ..< 3 {
         rawh = visible7 >= 14
      }
      if 4 == visible7 {
         visible7 /= Swift.max(((rawh ? 1 : 5) | (rowy ? 5 : 3)), 2)
      }
      while (templatesc.contains { $0 as? Int == visible7 }) {
         templatesc = [((rawh ? 3 : 1) >> (Swift.min(labs(visible7), 4)))]
         break
      }
      seede = [1]
      break
   }
        label.text = "Select Video"
        label.font = .systemFont(ofSize: 22, weight: .bold)
        label.textColor = .white
        label.textAlignment = .center
        return label
    }()

    private let uploadSubtitleLabel: UILabel = {
       var professionv: String! = String(cString: [101,115,101,110,100,0], encoding: .utf8)!
    _ = professionv
    var feedbackL: Int = 2
   withUnsafeMutablePointer(to: &feedbackL) { pointer in
    
   }
   if (professionv.count - 3) > 5 && (feedbackL - 3) > 4 {
       var reusef: Int = 5
       var colorE: Float = 0.0
      for _ in 0 ..< 2 {
         reusef &= (Int(colorE > 61422852.0 || colorE < -61422852.0 ? 83.0 : colorE))
      }
         reusef ^= (Int(colorE > 280901335.0 || colorE < -280901335.0 ? 26.0 : colorE) - reusef)
          var shoulderA: Double = 1.0
         colorE /= Swift.max(2, (Float(Int(shoulderA > 123547352.0 || shoulderA < -123547352.0 ? 92.0 : shoulderA))))
      if reusef == 2 {
         reusef &= (Int(colorE > 109292647.0 || colorE < -109292647.0 ? 39.0 : colorE))
      }
      if (Double(colorE + Float(reusef))) >= 5.1 {
         colorE -= Float(1 & reusef)
      }
          var hang3: String! = String(cString: [115,116,114,112,116,105,109,101,0], encoding: .utf8)!
          var lcopy_ad: Float = 5.0
         reusef -= (Int(colorE > 120036743.0 || colorE < -120036743.0 ? 23.0 : colorE) / 3)
         hang3 = "\((Int(lcopy_ad > 181024401.0 || lcopy_ad < -181024401.0 ? 98.0 : lcopy_ad)))"
         lcopy_ad += Float(2)
      feedbackL &= professionv.count - 3
   }

        let label = UILabel()
      feedbackL ^= professionv.count
        label.text = "Choose a throw video from Photos for on-device form check"
        label.font = .systemFont(ofSize: 15, weight: .regular)
        label.textColor = UIColor(white: 1, alpha: 0.85)
        label.textAlignment = .center
        label.numberOfLines = 0
        return label
    }()

    private let uploadButton: UIButton = {
       var u_managerC: String! = String(cString: [97,117,116,104,111,114,105,122,101,114,0], encoding: .utf8)!
    var areaf: Bool = true
      areaf = !areaf

        let button = UIButton(type: .custom)
      areaf = u_managerC.count < 64 && !areaf
        _ = UIImage(named: "analysis_button")
        button.setTitle("Select Video", for: .normal)
        button.setTitleColor(.white, for: .normal)
        button.titleLabel?.font = .systemFont(ofSize: 17, weight: .semibold)
        button.backgroundColor = UIColor(red: 150 / 255, green: 52 / 255, blue: 253 / 255, alpha: 1)
        button.layer.cornerRadius = 26.5
        button.clipsToBounds = true
      u_managerC = "\((u_managerC == (String(cString:[54,0], encoding: .utf8)!) ? u_managerC.count : u_managerC.count))"
        button.adjustsImageWhenHighlighted = false
        return button
    }()

    private let uploadHintLabel: UILabel = {
       var selM: [Any]! = [79, 0, 22]
   withUnsafeMutablePointer(to: &selM) { pointer in
          _ = pointer.pointee
   }
    var sessionG: Double = 0.0
   for _ in 0 ..< 2 {
      selM = [selM.count / (Swift.max(3, 1))]
   }

        let label = UILabel()
   for _ in 0 ..< 2 {
      sessionG /= Swift.max(5, (Double(3 + Int(sessionG > 122487091.0 || sessionG < -122487091.0 ? 32.0 : sessionG))))
   }
        label.text = "Supports MP4 and MOV · Max 200MB · Processed only on this device"
        label.font = .systemFont(ofSize: 12, weight: .regular)
        label.textColor = UIColor(white: 1, alpha: 0.45)
        label.textAlignment = .center
        label.numberOfLines = 0
        return label
    }()

    private let resultsTitleLabel: UILabel = {
       var fullS: Int = 4
   if 4 == fullS {
       var black9: String! = String(cString: [112,97,99,107,101,100,0], encoding: .utf8)!
       var launchT: Int = 0
       _ = launchT
       var field_: [Any]! = [21, 21, 38]
       var itemso: String! = String(cString: [97,112,112,114,111,120,105,109,97,116,105,111,110,0], encoding: .utf8)!
       var emailX: String! = String(cString: [112,115,121,109,111,100,101,108,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &emailX) { pointer in
    
      }
       var followedz: String! = String(cString: [97,100,100,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &followedz) { pointer in
             _ = pointer.pointee
      }
       var angleo: [Any]! = [93, 41]
       _ = angleo
       var buyt: [Any]! = [String(cString: [119,105,100,101,115,99,114,101,101,110,0], encoding: .utf8)!, String(cString: [115,98,119,97,105,116,0], encoding: .utf8)!]
      repeat {
          var visibleo: [String: Any]! = [String(cString: [109,117,108,116,105,116,97,98,108,101,0], encoding: .utf8)!:42, String(cString: [97,112,112,115,0], encoding: .utf8)!:52]
          var size_yv: Int = 5
         launchT /= Swift.max(3, followedz.count + itemso.count)
         visibleo["\(buyt.count)"] = visibleo.count
         size_yv ^= angleo.count
         if launchT == 3938245 {
            break
         }
      } while ((launchT % (Swift.max(field_.count, 4))) <= 1 || (field_.count % (Swift.max(1, 5))) <= 5) && (launchT == 3938245)
       var headerg: Bool = false
       _ = headerg
       var guidek: Bool = false
          var idsA: String! = String(cString: [115,102,116,112,0], encoding: .utf8)!
          var insets6: String! = String(cString: [115,117,98,108,97,121,101,114,115,0], encoding: .utf8)!
         guidek = insets6.count > 77
         idsA = "\(angleo.count & 2)"
      for _ in 0 ..< 2 {
         emailX = "\(1 << (Swift.min(2, followedz.count)))"
      }
          var selectionz: String! = String(cString: [115,109,111,111,116,104,0], encoding: .utf8)!
          _ = selectionz
          var pointE: Double = 4.0
         guidek = 100.41 > pointE || (String(cString:[121,0], encoding: .utf8)!) == black9
         selectionz = "\(3)"
         guidek = 8 == field_.count
      repeat {
         launchT &= followedz.count
         if 4253111 == launchT {
            break
         }
      } while (4253111 == launchT) && (black9.count > 3)
      if black9.count >= followedz.count {
         black9.append("\(((guidek ? 3 : 2)))")
      }
         guidek = (((guidek ? emailX.count : 67) >> (Swift.min(emailX.count, 1))) < 67)
       var usernamez: Bool = true
       _ = usernamez
       var recentR: Bool = false
         usernamez = !usernamez && guidek
         recentR = itemso.count < field_.count
      fullS /= Swift.max(black9.count, 1)
   }

        let label = UILabel()
        label.text = "Form check results"
        label.font = .systemFont(ofSize: 16, weight: .regular)
        label.textColor = .white
        return label
    }()

    private let resultsCardView: UIView = {
       var coinsR: String! = String(cString: [99,111,109,112,111,117,110,100,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &coinsR) { pointer in
          _ = pointer.pointee
   }
   while (2 <= coinsR.count || coinsR != String(cString:[97,0], encoding: .utf8)!) {
      coinsR.append("\(coinsR.count - coinsR.count)")
      break
   }

        let view = UIView()
        view.backgroundColor = UIColor(red: 32 / 255, green: 32 / 255, blue: 32 / 255, alpha: 1)
        view.layer.cornerRadius = 24
        view.layer.masksToBounds = true
        return view
    }()

    private let resultLabel: UILabel = {
       var ratioR: Double = 3.0
   withUnsafeMutablePointer(to: &ratioR) { pointer in
          _ = pointer.pointee
   }
    var delete_p13: String! = String(cString: [116,121,112,105,110,103,0], encoding: .utf8)!
   while ((4 ^ delete_p13.count) <= 4 || (4 & delete_p13.count) <= 5) {
       var resourceL: String! = String(cString: [107,95,55,57,0], encoding: .utf8)!
       var messageJ: String! = String(cString: [117,110,105,102,111,114,109,115,0], encoding: .utf8)!
       var cover3: String! = String(cString: [117,110,117,115,101,100,0], encoding: .utf8)!
      while (4 <= resourceL.count) {
          var scrollm: Double = 0.0
          var emailQ: String! = String(cString: [111,109,112,108,105,99,97,116,105,111,110,0], encoding: .utf8)!
          var commentsD: [Any]! = [[58, 16, 47]]
         resourceL.append("\(2 & cover3.count)")
         scrollm -= Double(3)
         emailQ.append("\(resourceL.count)")
         commentsD = [messageJ.count]
         break
      }
       var register_wpn: Double = 5.0
      withUnsafeMutablePointer(to: &register_wpn) { pointer in
    
      }
       var tapl: Double = 4.0
         register_wpn *= (Double(Int(tapl > 265177195.0 || tapl < -265177195.0 ? 86.0 : tapl) / (Swift.max(3, 6))))
         tapl -= (Double(messageJ.count << (Swift.min(1, labs(Int(tapl > 12210628.0 || tapl < -12210628.0 ? 68.0 : tapl))))))
      ratioR -= Double(3)
      break
   }

        let label = UILabel()
   repeat {
      ratioR -= Double(delete_p13.count)
      if 4080657.0 == ratioR {
         break
      }
   } while (4080657.0 == ratioR) && (delete_p13.count < 4)
        label.font = .systemFont(ofSize: 15, weight: .regular)
        label.textColor = UIColor(white: 1, alpha: 0.9)
        label.numberOfLines = 0
        return label
    }()
}


extension DV_ApplyController: PHPickerViewControllerDelegate {

@discardableResult
 func documentClearAppendModeControlLabel(containerDetected: Double, timesConstraint: Double, updatedWidth: String!) -> UILabel! {
    var passwordX: Double = 0.0
   withUnsafeMutablePointer(to: &passwordX) { pointer in
    
   }
    var c_positionU: String! = String(cString: [105,115,97,99,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &c_positionU) { pointer in
    
   }
   repeat {
      passwordX /= Swift.max((Double(Int(passwordX > 348290040.0 || passwordX < -348290040.0 ? 15.0 : passwordX) / (Swift.max(4, c_positionU.count)))), 2)
      if passwordX == 3510400.0 {
         break
      }
   } while (4.62 >= (Double(c_positionU.count) * passwordX)) && (passwordX == 3510400.0)
   repeat {
       var observationf: Double = 0.0
      withUnsafeMutablePointer(to: &observationf) { pointer in
             _ = pointer.pointee
      }
       var inew_p7: Bool = false
      withUnsafeMutablePointer(to: &inew_p7) { pointer in
    
      }
          var local_ca: [Any]! = [67, 42, 69]
          var stackV: [String: Any]! = [String(cString: [105,110,118,105,116,101,114,0], encoding: .utf8)!:String(cString: [97,110,105,109,97,116,111,114,115,0], encoding: .utf8)!]
         withUnsafeMutablePointer(to: &stackV) { pointer in
                _ = pointer.pointee
         }
          var contentl: Double = 5.0
         observationf *= Double(1)
         local_ca = [2]
         stackV["\(local_ca.count)"] = 2
         contentl -= (Double(local_ca.count ^ Int(contentl > 318613411.0 || contentl < -318613411.0 ? 5.0 : contentl)))
         inew_p7 = !inew_p7 || observationf < 89.53
      while (!inew_p7) {
         observationf -= (Double(Int(observationf > 157340401.0 || observationf < -157340401.0 ? 20.0 : observationf) | (inew_p7 ? 5 : 3)))
         break
      }
          var local_sd: String! = String(cString: [105,102,111,114,109,97,116,0], encoding: .utf8)!
         observationf += (Double(1 / (Swift.max(Int(observationf > 27069732.0 || observationf < -27069732.0 ? 89.0 : observationf), 5))))
         local_sd = "\(local_sd.count)"
         observationf -= (Double(Int(observationf > 340171889.0 || observationf < -340171889.0 ? 7.0 : observationf) ^ 1))
      repeat {
         observationf += (Double(3 - Int(observationf > 2725756.0 || observationf < -2725756.0 ? 43.0 : observationf)))
         if observationf == 41867.0 {
            break
         }
      } while (observationf == 41867.0) && (inew_p7)
      c_positionU = "\(c_positionU.count >> (Swift.min(labs(3), 3)))"
      if c_positionU.count == 2944363 {
         break
      }
   } while (c_positionU.count == 2944363) && (c_positionU.hasSuffix("\(passwordX)"))
     var likesSaved: Bool = true
     let characterFull: Double = 1.0
     var likedAmount: Double = 24.0
    var temporaryValUdio = UILabel(frame:CGRect.zero)
    temporaryValUdio.font = UIFont.systemFont(ofSize:12)
    temporaryValUdio.text = ""
    temporaryValUdio.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    temporaryValUdio.textAlignment = .center
    temporaryValUdio.alpha = 0.6;
    temporaryValUdio.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    temporaryValUdio.frame = CGRect(x: 310, y: 117, width: 0, height: 0)

    
    return temporaryValUdio

}






    func picker(_ picker: PHPickerViewController, didFinishPicking results: [PHPickerResult]) {

         let buildFmix: UILabel! = documentClearAppendModeControlLabel(containerDetected:77.0, timesConstraint:57.0, updatedWidth:String(cString: [109,111,100,117,108,101,109,97,112,0], encoding: .utf8)!)

      if buildFmix != nil {
          self.view.addSubview(buildFmix)
          let buildFmix_tag = buildFmix.tag
      }

_ = buildFmix


       var configL: Int = 2
   withUnsafeMutablePointer(to: &configL) { pointer in
          _ = pointer.pointee
   }
   repeat {
      configL += configL
      if 242929 == configL {
         break
      }
   } while (2 >= (configL + configL)) && (242929 == configL)

        picker.dismiss(animated: true)

        guard let itemProvider = results.first?.itemProvider,
              itemProvider.hasItemConformingToTypeIdentifier(UTType.movie.identifier) else {
            return
        }

        itemProvider.loadFileRepresentation(forTypeIdentifier: UTType.movie.identifier) { [weak self] url, _ in
            guard let self, let url else { return }

            let target = FileManager.default.temporaryDirectory
                .appendingPathComponent(UUID().uuidString)
                .appendingPathExtension(url.pathExtension.isEmpty ? "mp4" : url.pathExtension)

            do {
                if FileManager.default.fileExists(atPath: target.path) {
                    try FileManager.default.removeItem(at: target)
                }
                try FileManager.default.copyItem(at: url, to: target)

                DispatchQueue.main.async {
                    self.handleSelectedVideo(at: target)
                }
            } catch {
                DispatchQueue.main.async {
                    self.view.makeToast("Unable to load selected video")
                }
            }
        }
    }
}
